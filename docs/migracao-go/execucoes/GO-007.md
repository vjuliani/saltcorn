# GO-007 — Implementar tenancy e contexto transacional

## Tentativa GO-007-20260916T023544Z-01

- Executor / revisor: Claude Code (agente) / pendente
- Início / última atualização UTC: 2026-09-16T02:35:44Z / 2026-09-16T02:50:59Z
- Estado / motivo: DONE — PR revisado e integrado a `master` pelo autor do repositório
- Branch / base / commit / identificação do diff ou artefato: `task/go-007` / `master` (inclui GO-001..006 integrados) / `migracao/backend/internal/platform/{tenancy,database}/`
- URL e estado do PR / SHA validado / merge commit quando integrado: https://github.com/vjuliani/saltcorn/pull/10 (MERGED em 2026-09-16T02:55:04Z por vjuliani) / SHA validado `31df4be040b41cd772f9f40ba352e2543df49943` / merge commit `9b240d31a697f9c6ac13d9bc63df5c89ad8fcd33`
- Ambiente / versões / fixture: Go `1.22.2`; Postgres de teste **isolado e descartável** em container Docker próprio desta execução (mesmo padrão de GO-002), não o serviço do host nem containers de outros projetos
- Dependências verificadas e evidências: GO-005 DONE (fundação: `internal/platform/{config,health,shutdown}`, `cmd/{server,worker,cli}`) — esta tarefa estende a fundação, não a substitui; GO-006 DONE (contrato `internal-api.yaml` define `ServiceIdentity` com claims `{sub, tenant, iat, exp}` e a regra de que o backend valida `tenant` do token contra o `tenant` da URL — GO-007 implementa a propagação de contexto que torna essa checagem possível, a verificação criptográfica da assinatura do JWT fica para GO-008/GO-009); matriz GO-001 §2.1/§2.2 (Postgres: schema por tenant, `AsyncLocalStorage` a substituir por `context.Context`; risco de vazamento entre tenants em pool de conexões)
- Escopo reservado / proprietário de escrita: `migracao/backend/internal/platform/tenancy/` (novo), `migracao/backend/internal/platform/database/` (novo), `migracao/backend/cmd/server` e `cmd/worker` (estendidos para usar os pacotes novos), `docs/migracao-go/TASKS.md` (seção GO-007), `docs/migracao-go/tasks.csv` (linha GO-007), `docs/migracao-go/execucoes/GO-007.md`
- **Nota de escopo:** verificação criptográfica de assinatura do JWT de identidade delegada é GO-008/GO-009, não esta tarefa — aqui o token é decodificado (claims lidas) mas não verificado, e isso é documentado explicitamente no código, não escondido. GO-007 cobre resolução/propagação de tenant+ator+transação e isolamento de schema sob reuso de conexão — não cria nenhuma tabela de domínio (isso é GO-011).
- Subtarefas concluídas / pendentes:
  - [x] Preflight (dependências GO-005/GO-006 confirmadas DONE, git status, branch, PR existente)
  - [x] Registrar GO-006 como DONE (PR #9 já integrado)
  - [x] Criar branch `task/go-007`
  - [x] Preparar Postgres de teste isolado e descartável (dois schemas de tenant `acme`/`beta` com tabela-sonda `probe` para detectar vazamento)
  - [x] `internal/platform/tenancy`: contexto (tenant, ator), sanitização de identificador de schema (`SchemaName`, replica a regex de `sqlsanitize` de `db-common/internal.ts` — 16 testes cobrindo Unicode, dígito inicial, string vazia, caracteres perigosos)
  - [x] `internal/platform/database`: pool de conexões (`pgx/v5`), `WithTenant(ctx, tenant, fn)` executando `fn` dentro de uma transação com `SET LOCAL search_path` (nunca `SET` global — verificado experimentalmente, ver Checkpoint)
  - [x] Middleware HTTP (`tenancy.Middleware`): resolve tenant da URL (`r.PathValue`, roteamento por padrão do Go 1.22), decodifica (sem verificar assinatura, nota explícita no código) o token de identidade delegada, valida tenant do token contra o da URL (mesma regra do contrato GO-006 — 400/401/403 com os mesmos códigos de erro), injeta tenant+ator no `context.Context`
  - [x] Estendido `cmd/server` (rota de exemplo `/v1/tenants/{tenant}/tables/{table}/records`) e `cmd/worker` (`SALTCORN_GO_WORKER_TENANTS`) para propagar tenant/ator via `internal/platform/tenancy`, com banco opcional (`SALTCORN_GO_DATABASE_URL`) — sem tabela de domínio real (GO-011)
  - [x] Teste concorrente: 60 workers × 20 iterações alternando 2 tenants sobre pool de 3 conexões, sem vazamento de schema/dados
  - [x] Teste de cancelamento: contexto cancelado durante `pg_sleep` não deixa a conexão em estado inconsistente nem vaza `search_path` para o próximo uso (pool de 1 conexão, força a mesma conexão física)
  - [x] Teste de rollback: erro dentro de `WithTenant` desfaz a transação inteira, sem dado parcial
  - [x] Teste adicional (não planejado originalmente, adicionado ao descobrir que o teste de reuso concorrente não provava a necessidade de `SET LOCAL`): inspeciona `SHOW search_path` diretamente na conexão pooled após um `WithTenant` bem-sucedido — comprovadamente pega a regressão (`SET` sem `LOCAL`), verificado manualmente quebrando e restaurando o código
  - [x] Rodar `go build ./...`, `go vet ./...`, `gofmt -l .`, `go test -race -count=1 ./...` (com Postgres real) — todos limpos, evidência no Checkpoint
  - [x] Atualizar README de `migracao/backend/` (novos pacotes, variáveis de ambiente, como rodar os testes com Postgres local)
  - [x] Estender `.github/workflows/migracao-backend-ci.yml` com serviço Postgres efêmero + preparação dos schemas `acme`/`beta`, para a suíte de `database` rodar de verdade em CI
  - [ ] Sincronizar TASKS.md/tasks.csv, commit, push, abrir PR único (em andamento nesta tentativa)

### Validações

| Critério de aceite | Comando/procedimento e diretório | Resultado / exit code | Evidência / versão validada |
| --- | --- | --- | --- |
| Testes concorrentes com dois tenants não vazam schema/dados | `TestWithTenant_ConcurrentReuseNoLeak`: 60 goroutines × 20 iterações alternando `acme`/`beta` sobre pool de 3 conexões (Postgres real, isolado e descartável) | OK — 1200 operações, 0 mismatches | `internal/platform/database/database_test.go` |
| Reuso de conexão não vaza `search_path` | `TestWithTenant_DoesNotLeakSearchPathToNextConnectionUse`: `SHOW search_path` direto na conexão pooled (pool de 1) após commit de outro tenant | OK — e comprovadamente detecta a regressão: reproduzido trocando `SET LOCAL`→`SET` no código, o teste falhou (`search_path = "acme"`), restaurado e voltou a passar | idem; achado registrado no Checkpoint |
| Cancelamento não deixa a conexão/transação em estado inconsistente | `TestWithTenant_CancellationRollsBackWithoutLeaking`: contexto com timeout de 200ms + `pg_sleep(5)`; confirma erro e que a chamada seguinte (tenant diferente, mesma conexão física, pool de 1) funciona normalmente | OK | idem |
| Rollback não deixa dado parcial | `TestWithTenant_RollbackDiscardsWrites`: insert seguido de erro deliberado; confirma `count = 0` após | OK | idem |

### Checkpoint

- Data/hora UTC e estado consistente comprovado: 2026-09-16T02:50:59Z — implementação completa, 32 testes passando (incl. 5 contra Postgres real), CI estendido, README atualizado
- Operações persistentes / chaves de idempotência não sensíveis: nenhuma em banco de produção; três containers Postgres isolados e descartáveis foram usados ao longo da tarefa (um por rodada de verificação) e removidos ao final de cada uma (`docker rm -f`)
- Jobs/processos/leases ainda ativos: nenhum — binários de teste manual (`/tmp/claude-1000/go007-*`) executados e encerrados via `SIGTERM` (exit 0), fora do repositório
- Divergências, falhas e evidências invalidadas:
  - `pgx/v5` e `golang-jwt/jwt/v5` mais recentes exigem Go ≥1.24/1.25 (mesma situação de `oapi-codegen/runtime` em GO-006); fixados em `v5.7.2` e `v5.3.1` respectivamente, ambos confirmados compatíveis com Go 1.22 via `go.mod` de cada versão no proxy.
  - **Achado relevante, registrado por transparência:** o primeiro desenho do teste de reuso concorrente (`TestWithTenant_ConcurrentReuseNoLeak`) **não pegava** a regressão de `SET` vs. `SET LOCAL` — toda chamada via `WithTenant` redefine `search_path` no início da própria transação, mascarando o vazamento de sessão. Confirmado experimentalmente: com o código trocado para `SET` (sem `LOCAL`), esse teste continuou passando. Corrigido adicionando `TestWithTenant_DoesNotLeakSearchPathToNextConnectionUse`, que inspeciona o estado da conexão diretamente (bypassando `WithTenant`) — esse sim falha com `SET` e passa com `SET LOCAL`, verificado nos dois sentidos antes de aceitar a implementação como correta.
- Bloqueio e condição de desbloqueio: nenhum bloqueio
- Próximo passo exato e checks para retomada: nenhum para esta tentativa. GO-008 (identidade) substitui `ParseDelegatedIdentity`/`jwt.ParseUnverified` por verificação criptográfica real; GO-011 (metadados/schema) é o primeiro consumidor de `database.WithTenant` com tabelas de domínio de verdade
- Recuperação/rollback aplicável: não aplicável (nenhuma mutação em banco real; containers de teste descartados)
- Revisão / decisão de encerramento: PR #10 revisado e integrado a `master` por vjuliani em 2026-09-16T02:55:04Z (merge `9b240d31a697f9c6ac13d9bc63df5c89ad8fcd33`); tarefa encerrada como DONE

#### Evidência: validação local completa (Postgres real, isolado)

```
$ cd migracao/backend
$ gofmt -l . && go vet ./... && go build ./...
(sem saída — tudo limpo)

$ SALTCORN_GO_TEST_DATABASE_URL="postgres://postgres:postgres@127.0.0.1:PORT/saltcorn_test" \
    go test -race -count=1 ./...
ok  .../internal/platform/config     (7 testes)
ok  .../internal/platform/database   (5 testes, Postgres real)
ok  .../internal/platform/health     (3 testes)
ok  .../internal/platform/shutdown   (6 testes)
ok  .../internal/platform/tenancy    (16 testes)
# total: 37 testes, 0 falhas
```

#### Evidência: teste manual de ponta a ponta (server + worker)

```
$ SALTCORN_GO_DATABASE_URL=... ./server &
$ curl -H "Authorization: Bearer <jwt tenant=acme>" .../v1/tenants/acme/tables/guitars/records
{"tenant":"acme","actor":"42","db_time":"2026-09-16 02:47:32...+00"}   HTTP 200

$ curl -H "Authorization: Bearer <jwt tenant=acme>" .../v1/tenants/beta/tables/guitars/records
{"error":{"code":"tenant_mismatch",...}}                                HTTP 403

$ curl .../v1/tenants/acme/tables/guitars/records   # sem Authorization
{"error":{"code":"invalid_identity_token",...}}                         HTTP 401

$ kill -TERM <server pid> ; echo $?
0

$ SALTCORN_GO_DATABASE_URL=... SALTCORN_GO_WORKER_TENANTS=acme,beta ./worker &
saltcorn-go worker ... tenants=[acme beta]
$ kill -TERM <worker pid> ; echo $?
0
```
