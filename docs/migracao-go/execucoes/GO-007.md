# GO-007 — Implementar tenancy e contexto transacional

## Tentativa GO-007-20260916T023544Z-01

- Executor / revisor: Claude Code (agente) / pendente
- Início / última atualização UTC: 2026-09-16T02:35:44Z
- Estado / motivo: IN_PROGRESS — preflight concluído (incl. registro de GO-006 como DONE), desenhando tenancy/contexto transacional
- Branch / base / commit / identificação do diff ou artefato: `task/go-007` / `master` (inclui GO-001..006 integrados) / pendente
- URL e estado do PR / SHA validado / merge commit quando integrado: pendente
- Ambiente / versões / fixture: Go `1.22.2`; Postgres de teste **isolado e descartável** em container Docker próprio desta execução (mesmo padrão de GO-002), não o serviço do host nem containers de outros projetos
- Dependências verificadas e evidências: GO-005 DONE (fundação: `internal/platform/{config,health,shutdown}`, `cmd/{server,worker,cli}`) — esta tarefa estende a fundação, não a substitui; GO-006 DONE (contrato `internal-api.yaml` define `ServiceIdentity` com claims `{sub, tenant, iat, exp}` e a regra de que o backend valida `tenant` do token contra o `tenant` da URL — GO-007 implementa a propagação de contexto que torna essa checagem possível, a verificação criptográfica da assinatura do JWT fica para GO-008/GO-009); matriz GO-001 §2.1/§2.2 (Postgres: schema por tenant, `AsyncLocalStorage` a substituir por `context.Context`; risco de vazamento entre tenants em pool de conexões)
- Escopo reservado / proprietário de escrita: `migracao/backend/internal/platform/tenancy/` (novo), `migracao/backend/internal/platform/database/` (novo), `migracao/backend/cmd/server` e `cmd/worker` (estendidos para usar os pacotes novos), `docs/migracao-go/TASKS.md` (seção GO-007), `docs/migracao-go/tasks.csv` (linha GO-007), `docs/migracao-go/execucoes/GO-007.md`
- **Nota de escopo:** verificação criptográfica de assinatura do JWT de identidade delegada é GO-008/GO-009, não esta tarefa — aqui o token é decodificado (claims lidas) mas não verificado, e isso é documentado explicitamente no código, não escondido. GO-007 cobre resolução/propagação de tenant+ator+transação e isolamento de schema sob reuso de conexão — não cria nenhuma tabela de domínio (isso é GO-011).
- Subtarefas concluídas / pendentes:
  - [x] Preflight (dependências GO-005/GO-006 confirmadas DONE, git status, branch, PR existente)
  - [x] Registrar GO-006 como DONE (PR #9 já integrado)
  - [x] Criar branch `task/go-007`
  - [ ] Preparar Postgres de teste isolado e descartável (dois schemas de tenant com tabela-sonda para detectar vazamento)
  - [ ] `internal/platform/tenancy`: tipos e funções de contexto (tenant, ator), sanitização de identificador de schema (replicar `sqlsanitize` de `db-common/internal.ts`)
  - [ ] `internal/platform/database`: pool de conexões (pgx), `WithTenant(ctx, tenant, fn)` executando `fn` dentro de uma transação com `SET LOCAL search_path` — nunca `SET` global, para não vazar entre conexões do pool
  - [ ] Middleware HTTP: resolve tenant da URL, decodifica (sem verificar assinatura, nota explícita) o token de identidade delegada, valida que o tenant do token bate com o da URL (mesma regra do contrato GO-006), injeta tenant+ator no `context.Context`
  - [ ] Estender `cmd/worker` para propagar tenant/ator em jobs usando o mesmo `internal/platform/tenancy`
  - [ ] Testes concorrentes: dois tenants, pool pequeno forçando reuso de conexão, sem vazamento de schema/dados entre requisições concorrentes
  - [ ] Teste de cancelamento: contexto cancelado em meio a uma transação não deixa a conexão em estado inconsistente nem vaza `search_path` para o próximo uso
  - [ ] Teste de rollback: erro dentro de `WithTenant` desfaz a transação inteira, sem dado parcial
  - [ ] Rodar `go build ./...`, `go vet ./...`, `gofmt -l .`, `go test -race -count=1 ./...` e registrar evidência
  - [ ] Atualizar README de `migracao/backend/`
  - [ ] Sincronizar TASKS.md/tasks.csv, commit, push, abrir PR único

### Validações

| Critério de aceite | Comando/procedimento e diretório | Resultado / exit code | Evidência / versão validada |
| --- | --- | --- | --- |
| Testes concorrentes com dois tenants não vazam schema/dados | Goroutines concorrentes alternando entre dois tenants sobre um pool pequeno (força reuso de conexão); cada uma lê uma tabela-sonda e confirma que só vê o marcador do próprio tenant | Pendente | a preencher |
| Reuso de conexão não vaza `search_path` | Mesmo teste acima, mais um teste dedicado que verifica o estado do `search_path` de uma conexão reaproveitada após uma operação de outro tenant | Pendente | idem |
| Cancelamento não deixa a conexão/transação em estado inconsistente | Contexto com timeout curto + operação lenta (`pg_sleep`); confirma rollback e que a próxima operação (tenant diferente) funciona normalmente | Pendente | idem |
| Rollback não deixa dado parcial | `WithTenant` cuja função retorna erro após uma escrita; confirma que a escrita não persiste | Pendente | idem |

### Checkpoint

- Data/hora UTC e estado consistente comprovado: 2026-09-16T02:35:44Z — branch criada, registro de GO-006 já commitado, implementação ainda não escrita
- Operações persistentes / chaves de idempotência não sensíveis: nenhuma em banco de produção; Postgres de teste é isolado e descartável, removido ao final da tarefa
- Jobs/processos/leases ainda ativos: nenhum
- Divergências, falhas e evidências invalidadas: nenhuma
- Bloqueio e condição de desbloqueio: nenhum bloqueio no momento
- Próximo passo exato e checks para retomada: subir o Postgres de teste, escrever `tenancy` e `database`, então os testes concorrentes
- Recuperação/rollback aplicável: não aplicável ainda (nenhuma mutação além do ambiente de teste descartável)
- Revisão / decisão de encerramento: pendente
