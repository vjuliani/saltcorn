# GO-030 — Implementar adapter SQLite

## Tentativa GO-030-20260918T230000Z-01

- Executor / revisor: Claude Code (agente) / pendente
- Início / última atualização UTC: 2026-09-18T23:00:00Z / 2026-09-19T01:30:00Z
- Estado / motivo: IN_REVIEW — PR aberto, aguardando checks/revisão
- Branch / base / commit / identificação do diff ou artefato: `task/go-030` a partir de `origin/master` (GO-029 já integrada, PR #32, merge `40a3cb5dbb264b27bd5de424de950e95a9ed3b48`)
- URL e estado do PR / SHA validado / merge commit quando integrado: https://github.com/vjuliani/saltcorn/pull/33 (aberto, aguardando checks/revisão)
- Ambiente / versões / fixture: Go do módulo `migracao/backend/go.mod` (toolchain fixa em `go 1.22` — `modernc.org/sqlite` pinado em `v1.36.0`, a versão mais recente compatível: `v1.36.3`+ exige Go ≥1.25); Postgres 16 descartável via Docker (schemas `acme`/`beta`/`probe`) para a suíte Postgres já existente; SQLite via arquivos temporários (`t.TempDir()`), sem infraestrutura externa
- Dependências verificadas e evidências: GO-011 (catálogo de metadados, `internal/metadata`) DONE; GO-012 (compilador de consultas, `internal/records`) DONE; GO-013 (comandos de registro, `internal/records`) DONE; GO-014 (idempotência/outbox, `internal/platform/outbox`) DONE
- Escopo reservado / proprietário de escrita: `migracao/backend/internal/platform/database/tx.go` (novo), `migracao/backend/internal/platform/sqlite/` (novo), `migracao/backend/internal/metadata/{schema.go,catalog.go,version.go,catalog_test.go,concurrency_test.go,sqlite_test.go}` (extensão/novo), ~24 arquivos em `internal/{records,pack,views,triggers,expression,pluginhost}` e `cmd/{server,cli}` (extensão — só o argumento `tx` de chamadas a funções de `internal/metadata` envolvido em `database.AsTx(tx)`, nenhuma assinatura própria mudou), `migracao/backend/go.mod`/`go.sum`, `docs/migracao-go/inventario/GO-001-matriz-capacidades.md` §2.2, `migracao/backend/README.md`, `TASKS.md`/`tasks.csv`/`execucoes/GO-030.md`

### Decisões de escopo (preflight)

1. **Escopo confinado a `internal/metadata` — não "todo o backend fala com SQLite".** GO-030 depende explicitamente de GO-011/012/013/014 (não de tarefas posteriores) — lido como sinal de que só o catálogo de tabelas/campos (GO-011) precisa de prova concreta de dual-backend nesta entrega, não `internal/records` nem `internal/platform/outbox`. Investigação confirmou por quê os outros dois são estruturalmente mais caros: `internal/records` usa `xmin` (coluna de sistema do Postgres, sem NENHUM equivalente SQLite) para todo o mecanismo de concorrência otimista (`_version`); `internal/platform/outbox` usa `pg_advisory_xact_lock` e `FOR UPDATE SKIP LOCKED`, sem equivalente direto no modelo de escritor único do SQLite. Portar qualquer um dos dois exigiria uma decisão de design de concorrência própria — não uma generalização mecânica como a de `internal/metadata` (que não usa `xmin` nem `FOR UPDATE SKIP LOCKED` em lugar nenhum, confirmado por leitura completa do pacote antes de decidir o escopo).
2. **`database.Tx` é uma fronteira NOVA e MÍNIMA, não uma reescrita de `WithTenant`.** Alternativas descartadas: (a) mudar `WithTenant`/`WithTenantAndActor` para devolver a nova interface diretamente — rejeitada, porque isso propagaria a mudança de tipo para TODO pacote do backend (records, outbox, triggers, scheduler, notify, files, library, config, pack, views, cutover, realtime, identity — todos tipados contra `pgx.Tx` concreto), um refactor de dezenas de arquivos fora do orçamento desta tarefa e do seu próprio escopo declarado; (b) manter `internal/metadata` só contra Postgres e criar uma reimplementação SQLite paralela — rejeitada por violar DRY (duas fontes de verdade para a mesma regra de negócio). Escolhida: `database.AsTx(tx pgx.Tx) Tx` adapta uma transação Postgres JÁ ABERTA (por `WithTenant`, sem NENHUMA mudança) no ponto de CHAMADA de `internal/metadata` — confinando o impacto a "envolver `tx` com `database.AsTx(tx)` nos ~24 arquivos que chamam funções de `internal/metadata`", nunca a uma mudança de assinatura de nenhuma outra função pública.
3. **`modernc.org/sqlite` (puro Go, sem cgo) em vez de `mattn/go-sqlite3` (cgo).** Confirmado por teste de compatibilidade de versão ANTES de adicionar a dependência: a versão mais recente (`v1.59.0`) exige Go ≥1.25; este módulo fixa a toolchain em `go 1.22` deliberadamente (README, "evitar exigir uma versão de Go mais nova que a documentada"). Pinado em `v1.36.0` — a mais recente release cujo `go.mod` ainda declara `go 1.21` (a seguinte, `v1.36.3`, já pula para `go 1.23.0`). Confirmado por smoke test isolado (fora do módulo, antes de qualquer código de produção) que `$1`/`$2` (placeholders posicionais do Postgres), `RETURNING`, e `INTEGER PRIMARY KEY AUTOINCREMENT` funcionam exatamente como esperado com este driver.
4. **"Modo desktop" = arquivo por tenant, sem pool — mesma decisão do legado (`packages/sqlite/sqlite.ts`), nunca um pool multi-tenant como o Postgres.** `internal/platform/sqlite.DB` nunca abre mais de UMA conexão física por arquivo (`SetMaxOpenConns(1)`) — decisão que também resolve "concorrência" (ver item 6) sem precisar de nenhum mecanismo de lock explícito.
5. **Só 2 divergências de sintaxe reais, confirmadas empiricamente, não a ampla divergência que a hipótese inicial temia.** `$N`, `RETURNING`, `ON CONFLICT ... DO NOTHING`, e a maioria dos nomes de tipo (`text`/`int`/`boolean`/`smallint`/`bigint` — SQLite não rejeita nomes de tipo desconhecidos, só os usa para inferir afinidade) funcionam sem NENHUMA mudança nos dois SGBDs. As duas divergências reais: (a) `id serial PRIMARY KEY` (Postgres) vs `id INTEGER PRIMARY KEY AUTOINCREMENT` (SQLite, literal exato exigido); (b) `ALTER TABLE ... DROP COLUMN IF EXISTS` — SQLite suporta `DROP COLUMN` desde 3.35, mas SEM `IF EXISTS`. A segunda foi um achado REAL durante a implementação (não uma regressão deliberada — o primeiro corpus de teste SQLite já pegou isso na primeira rodada, antes de qualquer verificação deliberada).
6. **`lockCatalog` diverge por DESIGN no SQLite, não por sintaxe.** `pg_advisory_xact_lock` (Postgres) vira um no-op deliberado no SQLite: como `SetMaxOpenConns(1)` já serializa estruturalmente qualquer mutação do MESMO tenant antes mesmo de `lockCatalog` rodar, um mecanismo de lock explícito seria redundante. Confirmado por regressão deliberada (10 `CreateTable` concorrentes sobre o MESMO arquivo, `_sc_metadata_version` termina em exatamente 10 com `SetMaxOpenConns(1)`; falha real com `SQLITE_BUSY` sem ele).
7. **`internal/pack`/`internal/pluginhost`/`internal/triggers`/`internal/views` etc. permanecem Postgres-only** — a mudança que os afeta é só "o argumento que passam para `internal/metadata` precisa ser `database.AsTx(tx)` em vez de `tx`", nunca uma mudança de comportamento ou de dependência de SQLite.
8. **Verificação de regressão deliberada, dois pontos** (mais um achado real não-deliberado, ver item 5b): (a) branch de `idColumnDDL` removido — `CreateTable` falha de verdade já na primeira linha de `_sc_tables` (`converting NULL to int is unsupported`); (b) `SetMaxOpenConns(1)` removido — 10 goroutines concorrentes falham de verdade com `SQLITE_BUSY` (não uma simulação).

### Subtarefas concluídas / pendentes

- [x] Preflight (leitura completa de `internal/metadata` para confirmar ausência de dependência de `xmin`/advisory-lock-específico; smoke test isolado de compatibilidade Go 1.22 + `$N`/`RETURNING`/autoincremento antes de decidir a dependência; decisões de escopo fixadas)
- [x] Criar branch `task/go-030`
- [x] `go.mod`: adicionar `modernc.org/sqlite v1.36.0` (`go mod tidy`)
- [x] `internal/platform/database/tx.go` (novo): `Tx`/`Row`/`Rows`/`Dialect`/`ErrNoRows`/`AsTx()`
- [x] `internal/platform/sqlite/` (novo): `db.go` (Open/WithTenant/Close/FilePath, arquivo por tenant, `SetMaxOpenConns(1)`), `tx.go` (`Tx`/`sqlRow`/`sqlRows`) + corpus de testes (6 testes: commit/rollback, isolamento entre arquivos de tenant, concorrência sem perda de escrita, `ErrNoRows` neutro)
- [x] `internal/metadata`: `schema.go` (DDL dialeto-consciente via `idColumnDDL`/`timestampDefaultDDL`), `catalog.go`/`version.go` (assinaturas `pgx.Tx` → `database.Tx`, `lockCatalog` no-op para SQLite, `DROP COLUMN` dialeto-consciente), `catalog_test.go`/`concurrency_test.go` (chamadas envolvidas em `database.AsTx(tx)`), `sqlite_test.go` (novo: 8 testes espelhando os cenários de `catalog_test.go`/`concurrency_test.go` contra SQLite)
- [x] Atualizar ~24 arquivos chamadores de `internal/metadata` (records, pack, views, triggers, expression, pluginhost, cmd/server, cmd/cli) para `database.AsTx(tx)` — nenhuma assinatura própria mudou
- [x] Verificação de regressão deliberada: 2 pontos (branch de id autoincrementado, `SetMaxOpenConns(1)`) — mais 1 achado real não-deliberado (`DROP COLUMN IF EXISTS`)
- [x] Atualizar `inventario/GO-001-matriz-capacidades.md` (linha "Adapter SQLite" §2.2)
- [x] README (`migracao/backend/README.md`, nova seção "Adapter SQLite (GO-030)")
- [x] Rodar builds/testes completos dos pacotes afetados (Go com `-race`, Postgres real + SQLite)
- [x] Sincronizar TASKS.md/tasks.csv, commit, push, abrir PR único ([#33](https://github.com/vjuliani/saltcorn/pull/33))

### Validações

| Critério de aceite | Comando/procedimento e diretório | Resultado / exit code | Evidência / versão validada |
| --- | --- | --- | --- |
| Mesmas fixtures de domínio passam PG/SQLite com divergências justificadas | `go test -race -run TestSQLite ./internal/metadata/...` (SQLite) e `go test -race ./internal/metadata/...` (Postgres real) em `migracao/backend` | PASS (exit 0) nos dois | `internal/metadata/sqlite_test.go` (8 testes, espelhando `catalog_test.go`/`concurrency_test.go`: `CreateTable` cria tabela física com autoincremento real, idempotência, `AddField` com `FieldKey`/referência física funcionando, `ListTables`/`ListFields`, `DropField`/`DropTable` idempotentes, `CurrentVersion`) — suíte Postgres original inalterada e verde |
| Concorrência e recuperação são verificadas sem sintaxe exclusiva de PG | `go test -race -run TestWithTenant_Concorrencia\|TestSQLite_Concorrencia ./internal/platform/sqlite/... ./internal/metadata/...` | PASS (exit 0) | `TestWithTenant_ConcorrenciaMesmoTenant_SerializaSemPerderEscrita` (20 goroutines incrementando um contador, `internal/platform/sqlite`); `TestSQLite_ConcorrenciaMesmoTenant_VersaoNuncaPerdeIncremento` (10 `CreateTable` concorrentes, `internal/metadata`) — nenhum usa `pg_advisory_xact_lock`/`FOR UPDATE SKIP LOCKED`; `TestWithTenant_ErroDesfazTransacao` prova recuperação (rollback desfaz completamente) |

Suítes completas rodadas nesta tentativa, todas verdes:

- Backend Go: `go build ./... && go vet ./... && gofmt -l . && go test -race -count=1 ./...` em `migracao/backend` (Postgres real, fixture `acme`/`beta`/`probe` + fixture própria de `internal/metadata`/`internal/platform/sqlite`) — todos os pacotes OK, incluindo `internal/platform/sqlite` (6 testes novos) e `internal/metadata` (8 testes novos em `sqlite_test.go`, mais os já existentes contra Postgres, todos passando sem alteração de comportamento).

### Achado de verificação (regressão deliberada, dois pontos, mais um achado real)

1. **`idColumnDDL` (`internal/metadata/schema.go`)**: removido o branch de dialeto, sempre retornando `"id serial PRIMARY KEY"`. `TestSQLite_CreateTable_CriaTabelaFisicaComAutoincremento` falhou já no PRIMEIRO passo (inserir em `_sc_tables`, que também usa `idColumnDDL`): `metadata: inserir tabela no catálogo: sql: Scan error on column index 0, name "id": converting NULL to int is unsupported` — confirmando que, sem o literal exato `INTEGER PRIMARY KEY AUTOINCREMENT`, o SQLite genuinamente não gera nenhum valor de id. Restaurado, suíte volta a passar.
2. **`SetMaxOpenConns(1)` (`internal/platform/sqlite/db.go`, `connFor`)**: removida a chamada. `TestWithTenant_ConcorrenciaMesmoTenant_SerializaSemPerderEscrita` (20 goroutines) falhou de forma consistente em 3 execuções seguidas com `database is locked (5) (SQLITE_BUSY)` — confirmando que, sem uma única conexão por arquivo, o SQLite genuinamente rejeita escritas concorrentes da mesma forma que rejeitaria em produção. Restaurado, suíte volta a passar.
3. **Achado real (não uma regressão deliberada) — `ALTER TABLE ... DROP COLUMN IF EXISTS` (`internal/metadata/catalog.go`, `DropField`)**: a primeira execução do corpus SQLite (antes de qualquer verificação deliberada) já falhou com `SQL logic error: near "EXISTS": syntax error` — SQLite suporta `DROP COLUMN` desde a versão 3.35, mas SEM a cláusula `IF EXISTS` (só Postgres tem essa variante). Corrigido com um branch de dialeto na construção da string de DDL (`dropColumnDDL`). Confirmado por regressão adicional (reintroduzindo `IF EXISTS` incondicional): a mesma falha reaparece de forma idêntica.

### Checkpoint

- Data/hora UTC e estado consistente comprovado: 2026-09-19T01:30:00Z — implementação completa (`internal/platform/database/tx.go`, `internal/platform/sqlite/`, extensão de `internal/metadata` e ~24 chamadores), matriz e README atualizados, todas as suítes verdes (Postgres inalterado + SQLite novo)
- Operações persistentes / chaves de idempotência não sensíveis: nenhuma em banco de produção; Postgres de teste (container Docker descartável, porta 55572) a ser removido ao final; arquivos SQLite de teste em diretórios temporários (`t.TempDir()`), removidos automaticamente pelo Go
- Jobs/processos/leases ainda ativos: nenhum
- Divergências, falhas e evidências invalidadas: nenhuma
- Bloqueio e condição de desbloqueio: nenhum bloqueio no momento
- Próximo passo exato e checks para retomada: aguardar revisão/checks do PR [#33](https://github.com/vjuliani/saltcorn/pull/33); após merge, marcar GO-030 como DONE em `TASKS.md`/`tasks.csv`/`execucoes/GO-030.md` com o SHA de merge
- Recuperação/rollback aplicável: não aplicável (nenhuma mutação em ambiente compartilhado; container/arquivos de teste são locais e descartáveis)
- Revisão / decisão de encerramento: pendente (aguardando PR)

## Tentativa GO-030-20260918T233350Z-02

- Executor: Codex; revisão pendente. Retomada da tentativa anterior.
- Estado: IN_PROGRESS; branch `task/go-030`, base `master`, HEAD `8dc3ea094c63d76a6657f80accdaaee04cd73c44`, árvore inicialmente limpa.
- PR único: https://github.com/vjuliani/saltcorn/pull/33 (OPEN, CI anterior SUCCESS, sem revisão).
- Ambiente: Go 1.22.2, fixtures SQLite temporárias e PostgreSQL descartável.
- Dependências GO-011/012/013/014 integradas na base; fetch confirmou estado remoto.
- Escopo reservado: adapter SQLite, abstração de transação, records, outbox, fixtures e documentação.
- Revisão do checkpoint: a restrição anterior a metadata não atende ao escopo de outbox e fixtures de domínio da GO-030. Evidências anteriores cobrem somente o subconjunto implementado; não comprovam aceite integral.
- Achado: WithTenant pode commitar durante panic do callback; reproduzir e corrigir com rollback garantido.
- Checks previstos: testes de falha/rollback/concorrência/isolamento, fixtures compartilhadas PG/SQLite, build, vet e suíte Go com race.
- Operações de produção: nenhuma; sem jobs desta tentativa em execução.
- Próximo passo: implementar correções e validar no mesmo PR, sem merge automático.

### Implementação e decisões da retomada

- A decisão anterior de excluir records/outbox foi substituída: ambos usam agora `database.Tx`, com entradas `*Tx` e wrappers compatíveis com os consumidores pgx existentes. Nenhum chamador de servidor/triggers precisou mudar sua assinatura.
- SQLite usa `BEGIN IMMEDIATE` antes da primeira leitura e `busy_timeout(5000)`. Uma conexão por instância reduz contenção local; a reserva de escritor protege também handles/processos distintos. Outbox grava efeito/chave/evento na mesma transação, usa savepoints por tentativa e mantém retries/falha terminal.
- `_version` explícita no SQLite; PostgreSQL mantém xmin. `metadata.EnsureSchema` acrescenta a coluna a tabelas do checkpoint anterior sem apagar registros. Conflito com campo de usuário reservado exige resolução explícita.
- DDL de campo único usa índice no SQLite. Datas são UTC com precisão de microssegundos, booleanos retornam bool e ordenação de NULL acompanha PostgreSQL. Diferenças de representação de integer/timezone e folding ASCII de Like estão registradas no README.
- `WithTenant` em ambos os adapters confirma apenas após retorno normal do callback e garante rollback em panic/Goexit. Falha reproduzida antes da correção: `TestWithTenant_PanicDesfazTransacao` persistia 1 registro; depois, PASS.
- A fixture compartilhada encontrou bool SQLite retornado como inteiro; corrigido no adapter. A comparação de datas foi ajustada para comparar o instante (PostgreSQL local retornou timezone -03:00), preservando a representação existente do PostgreSQL.
- CSV anterior tinha uma vírgula sem aspas em `proximo_passo`, deslocando branch/base/URL/comando para colunas erradas. A linha GO-030 foi corrigida, mantendo o comando completo; checkbox desmarcado enquanto não houver merge.
- Limite da entrega: capacidades GO-011/012/013/014 via adapter desktop. Servidor HTTP, RLS nativa e módulos posteriores permanecem integrados a PostgreSQL; GO-031/032 tratam sync/distribuição. Não há declaração de migração global concluída.

### Validações da retomada

Ambiente: Go 1.22.2 linux/amd64, PostgreSQL 16.15 em container existente `saltcorn-go030-pg` (contexto Docker `default`), banco **novo e descartável** `go030_parity`, porta 55572. Schemas acme/beta/probe preparados conforme README; papel de teste `go030_review_rls` NOSUPERUSER/NOBYPASSRLS para RLS. Nenhum banco de produção acessado. SQLite usa `t.TempDir()` e subprocessos do binário de teste.

| Critério | Comando/procedimento em `migracao/backend` | Resultado |
| --- | --- | --- |
| Mesmas fixtures de domínio | `SALTCORN_GO_TEST_DATABASE_URL=postgres://postgres:postgres@127.0.0.1:55572/go030_parity go test -race ./internal/platform/sqlite -run Parity -count=1` | PASS, exit 0: mesmos testes executados para SQLite e PostgreSQL; CRUD, tipos, constraints, autorização de escrita, versão obsoleta, injeção, joins, paginação e rollback de hook |
| Concorrência/idempotência | `TestParityOutbox` na mesma suíte | PASS: oito chamadas concorrentes, dois handles SQLite para o mesmo arquivo; efeito executado uma vez; payload diferente rejeitado; rollback remove efeito/chave/evento; savepoint desfaz efeito do handler |
| Recuperação e upgrade | `go test -race ./internal/platform/sqlite -count=1` com DSN de teste; também coberto pela suíte completa final | PASS: subprocesso encerrado por `os.Exit(23)` antes/depois do commit, recuperação e replay; upgrade executado duas vezes preserva dados locais; falha terminal após limite de tentativas |
| Rollback em panic | `TestWithTenant_PanicDesfazTransacao` e `TestParityPanicRollback` | Falha reproduzida antes do fix; PASS após correção nos dois adapters |
| Compatibilidade completa e RLS | `SALTCORN_GO_TEST_DATABASE_URL=postgres://postgres:postgres@127.0.0.1:55572/go030_parity SALTCORN_GO_TEST_DATABASE_URL_RLS=postgres://go030_review_rls:go030_review_rls@127.0.0.1:55572/go030_parity go test -race -count=1 ./...` | PASS, exit 0: 28 pacotes com testes, mais cmd/worker sem testes; saída em [GO-030-validacao.txt](GO-030-validacao.txt) |
| Compilação/análise/formatação | `go build ./...`; `go vet ./...`; `gofmt -l .` | PASS, exit 0; nenhuma saída de gofmt |
| Controle documental | Parse CSV, IDs únicos, estados/dependências válidos, DFS sem ciclos, alinhamento de branch/base/PR/comando e checkbox de GO-030; `git diff --check` | PASS |

### Checkpoint da retomada — 2026-09-18T23:48:53Z

- Estado: VALIDATING; código e testes completos, revisão local do diff concluída; revisão externa pendente.
- Evidência: árvore desta tentativa sobre `8dc3ea094c63d76a6657f80accdaaee04cd73c44`; o commit de implementação desta seção identifica o diff validado. Alterações posteriores restritas ao registro de publicação não invalidam testes.
- Horário da tentativa anterior (2026-09-19T01:30Z) é posterior ao relógio desta sessão; preservado como histórico, sem reutilizá-lo para datar esta validação.
- Operações: fixtures descartáveis; nenhum job/lease de produção. Container de teste preexistente preservado.
- Próximo passo: commit/push e atualizar título/descrição do PR único #33; registrar SHA e estado IN_REVIEW. Aguardar revisão/CI e merge antes de DONE.
- Rollback: reverter o commit de implementação antes do corte; para arquivos SQLite com dados, preservar cópia consistente e não apagar a coluna de versão como procedimento de reversão. Nenhum corte/deploy realizado.
