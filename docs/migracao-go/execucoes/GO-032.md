# GO-032 — CLI e distribuição self-hosted

## Tentativa GO-032-20260919T163236Z-01

- Executor: Codex; revisor externo pendente. Estado IN_PROGRESS.
- Base: master `552297dafc30419bb114f14717b032cdc53aa4c7`; branch `task/go-032`; checkout inicial limpo, sem PR anterior.
- Dependências GO-005 (#8), GO-027 (#30), GO-030 (#33) DONE e presentes na base. Correções de CI #35 integradas, checks do PR aprovados.
- Ambiente inicial: Linux amd64, Go 1.22.2, Node 22.23.2; nenhum container em execução no contexto Docker default. Fixtures novas, sem dados de produção.
- Reserva de escrita: CLI/serviços operacionais Go, empacotamento em migracao/distribution, integração de assets/sessão operacional BFF, fixtures e CI, documentação da task.
- Subtarefas: setup não destrutivo e migrations versionadas; configuração e inventário de plugins; backup/restore consistente com validação e destino vazio; supervisão serve/health; release com backend/BFF/frontend e manifesto compatível; testes de instalação/upgrade/recuperação PG/SQLite.
- Perfil web PostgreSQL; perfil SQLite restrito às capacidades já portadas (metadata/records/outbox/sync), sem declarar domínio web inteiro portado. Runtime mobile JS preservado; não distribuir binário Go em aparelhos.
- Checks planejados: Go build/vet/race e fixtures PG/SQLite; BFF/frontend build/tests; build do pacote e smoke HTTP dos processos reais; falhas de restore/versão/lock/integridade; CI e consistência CSV/Markdown.
- Próximo passo: implementar e validar, publicar um único PR; sem merge/deploy automático.

## Implementação e primeira validação

- CLI operacional e serviço `internal/installation` implementados; journal schema 1→2, diretório privado/ID, locks locais e PostgreSQL, setup retomável, backup/restore em destino vazio, configuração JSON e inventário de plugins. Novo endpoint opcional de sessão administrativa consta no OpenAPI.
- Release Linux com três binários Go, BFF e assets React/SB Admin 2/Craft.js; build isolado por lockfiles, manifesto de versões e hashes, supervisão/health e runbook. Frontend lockfile passa a ser versionado para `npm ci` reproduzível em CI.
- Perfil web PG e perfil administrativo SQLite documentados em ADR-0012, sem extrapolar suporte de domínio/web/mobile. Código Go não vai para o aparelho; builds nativos não executados.
- Falhas de desenvolvimento preservadas: primeiro teste usou nome/tipo errado da API metadata (corrigido para table.ID/FieldText); primeiro `pg_dump` recebeu PGSERVICE vazio e tentou resolver serviço inexistente (agora remove PG* herdadas e monta ambiente explícito); primeiro smoke usou URI em PGDATABASE sem parâmetros libpq explícitos (fixture corrigida); frontend sem escalada falhou EPERM ao abrir sockets realtime (reexecutado com permissão, 40/40 aprovados).
- Recuperação PostgreSQL/SQLite aprovada: dados, arquivo, configuração, senha/papel admin, plugin 1.2.3, fila pendente e idempotency key preservados; upgrade idempotente, rollback em DDL conflitante, versão futura, corrupção, symlink, identidade errada, destino ocupado e locks recusados.
- Primeiro pacote completo construído em `/tmp/go032-release`; smoke HTTP com processos reais aprovou setup/migrate/check, assets, sessão, CSRF, replay negado, bloqueio de backup com serve ativo, restauração e reinício/reautenticação. Alterações finais de guards/retomada serão verificadas no pacote final.
- Regressão backend `go test -race -count=1 ./...` com PostgreSQL descartável e `SALTCORN_GO_TEST_MOBILE_E2E=1`: exit 0; build/vet aprovados. BFF 50 testes, frontend 40 testes, contratos lint/geração/typecheck/bundle e cliente Go build/vet/race aprovados. Actionlint da CI distribuição aprovado.

## Retomada GO-032-20260919T221457Z-02

- Data UTC: 2026-09-19T22:14:57Z; executor Codex; mesma branch/base e alterações preservadas, nenhum PR anterior. Revisão externa pendente; estado VALIDATING.
- Interrupção anterior: revisão automática de permissão expirou antes do build final; retry autorizado iniciou e concluiu o build. A chamada de testes seguinte foi rejeitada pela revisão automática por limite de uso, sem executar. Retomada solicitada pelo usuário; testes pendentes executados com aprovação e concluídos.
- Revisão final: separado o ambiente do BFF para remover credenciais SALTCORN_GO_* e PG*. Smoke lê somente o ambiente dos próprios processos de teste e comprova ausência de DSN/SMTP/PGPASSWORD no BFF. Ele usa apenas API interna e identidade delegada.
- Pacote final `/tmp/go032-final-release`, construído em árvore isolada; CLI recompilada com os mesmos flags após ajuste do ambiente, manifesto regenerado e smoke repetido com sucesso. Nenhum artefato compilado ou segredo real incluído no Git.

| Critério | Comando e ambiente | Resultado |
| --- | --- | --- |
| Instalação limpa e upgrade | `go test -race -count=1 -v ./internal/installation ./cmd/cli`, em `migracao/backend`, DSN administrativa de PG16 descartável em 55573 | PASS; fixtures PG/SQLite, versão 1→2, idempotência, journal e rollback. Testes e2e-seed não repetidos nesse comando sem DSN geral; já aprovados na regressão completa |
| Dados/arquivos/config/plugins | Mesmo teste: `TestInstallationRecoveryParity` | PASS; senha e papel admin PG, registros/sequence, arquivo, config, plugin 1.2.3, evento pending e idempotency key preservados; rejeita corrupção, destino ocupado, versão futura, links e identidade errada |
| CLI/pacote completo | `migracao/distribution/build.sh /tmp/go032-final-release`; `node migracao/distribution/smoke.mjs /tmp/go032-final-release`, DSN via SALTCORN_GO_TEST_INSTALLATION_DATABASE_URL | PASS; setup, retomada após commit, migrate, check, assets, sessão/CSRF/replay, lock, credenciais isoladas, backup/restore, reinício/reautenticação e recusa SQLite web/manifesto incompatível |
| Regressão Go e mobile | `go test -race -count=1 ./...` com SALTCORN_GO_TEST_DATABASE_URL (go032_regression), SALTCORN_GO_TEST_INSTALLATION_DATABASE_URL e SALTCORN_GO_TEST_MOBILE_E2E=1 | PASS; build/vet completos também aprovados; nenhum teste nativo Android/iOS declarado |
| BFF/frontend | `npm test --prefix migracao/packages/bff`; equivalente frontend, com sockets locais permitidos | PASS; 50 testes BFF, 40 frontend |
| Contrato opcional de sessão | `npm run validate && npm run bundle:bff`; oapi-codegen v2.8.0 para BFF; `go build ./... && go vet ./... && go test -race -count=1 ./...` em contracts/gen/go | PASS; lint, TS e cliente Go |
| CI e scripts | actionlint v1.7.7 no workflow distribuição, `bash -n migracao/distribution/build.sh`, gofmt e git diff --check | PASS |

Evidências resumidas: [GO-032-validacao.txt](GO-032-validacao.txt). Logs completos temporários em `/tmp/go032-*`. Go 1.22.2, Node 22.23.2, PostgreSQL/client tools 16.15. Container exclusivo `saltcorn-go032-pg`, porta 55573; bancos de instalação/smoke criados com nomes exclusivos e removidos pelos testes. Banco go032_regression permanece até limpeza final. Nenhum processo serve desta tarefa permanece ativo.

Próximo passo: commit da árvore validada, push e PR único; acompanhar os checks remotos. Sem merge/deploy. Limites e recuperação em ADR-0012/runbook; não declarar migração integral de SQLite web, login final de usuários ou builds nativos mobile.

## Publicação — 2026-09-19T22:17:22Z

- Estado IN_REVIEW; PR único [#36](https://github.com/vjuliani/saltcorn/pull/36), OPEN, `task/go-032` → `master`.
- Commit de implementação e validação: `c3310ad20e7c22f2f37e11d1e1c1395dd2deaf90`. Atualização posterior de acompanhamento apenas documental.
- No momento da abertura, checks `build-test`, `lint-and-ts-client`, `go-client`, `offline-sync` e `install-upgrade-restore` IN_PROGRESS. Resultado final será registrado na descrição do PR, vinculado ao SHA, sem reiniciar os checks por novos commits documentais.
- Nenhum processo serve local permanece ativo. Artefato de revisão local em `/tmp/go032-final-release`; CI da distribuição também publica tar.gz quando aprovada. Revisão técnica externa pendente; sem merge/deploy automático.

## Reconciliação na GO-033 — 2026-09-19T22:29:00Z

PR #36 MERGED, merge `5eeca039f163fa55563ab32e28dffa2d88e76fdc` presente na base. Checks aplicáveis SUCCESS; deploy documental do PR #35 SKIPPED por desenho. Estado reconciliado para DONE. Revisão comprovada pela integração.
