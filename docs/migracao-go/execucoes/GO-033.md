# GO-033 — Executar matriz completa de paridade

## Tentativa GO-033-20260919T222842Z-01

- Executor: Codex; revisão externa pendente.
- Início UTC: 2026-09-19T22:28:42Z; estado IN_PROGRESS.
- Branch `task/go-033`, base `master` em `5eeca039f163fa55563ab32e28dffa2d88e76fdc`; checkout inicialmente limpo, nenhum PR anterior.
- Dependências GO-021/025/026/027/028/029/030 integradas; GO-031/032 reconciliadas após consulta GitHub/ancestralidade/checks.
- Reserva exclusiva: `migracao/parity`, harness E2E, workflow consolidado, lockfiles de testes, documentação da GO-033.
- Ambiente: Linux, Go 1.22.2, Node 22.23.2, PG/client 16.15. Fixture planejada exclusiva `saltcorn-go033-pg`/55574; nenhum dado de produção.
- Preservar prévia em 3100/8090, PostgreSQL `saltcorn-preview-pg`/55573 e arquivos `/tmp/saltcorn-preview`.

### Subtarefas e checks previstos

1. Matriz de todas as linhas do inventário GO-001, com limites e evidências por capacidade, perfis piloto/integral distintos.
2. Runner reproduzível: contratos TS/Go, backend race PG/SQLite/RLS, BFF/frontend, host JS, cliente offline, UI Chromium/Firefox e instalação/upgrade/restore do pacote real.
3. Gates que rejeitam evidências ausentes, skips inesperados e lacunas aplicáveis; separar execução das suítes de autorização de promoção.
4. Executar, investigar falhas, registrar hashes/versões/resultados e publicar um único PR.

### Checkpoint

2026-09-19T22:29:00Z: preflight concluído. Nenhuma escrita em bancos existentes. Próximo passo: implementar matriz/runner e preparar fixture descartável. A promoção do piloto continua condicionada às lacunas reais de integração; GO-033 não declara GO-034/035/036 cumpridas.

## Retomada — 2026-09-20T11:14:00Z

- Mesma tentativa e branch; nenhum PR GO-033 existente, base remota inalterada. Prévia `http://127.0.0.1:3100/healthz` respondeu 200; container/arquivos preservados.
- A execução autorizada antes da interrupção terminou em 2026-09-19T22:38:44Z: oito suítes PASS, audit exit 0; `/tmp/go033-parity-01`, inputs SHA-256 `cfd0276a1e9d9a883d30dc9af678de68bbe1a88ea01558751010e41fce0d1cf1`.
- Houve recusa da revisão automática para iniciar checks adicionais por limite de uso; não houve tentativa de contornar. Com o pedido de continuar, os checks foram autorizados normalmente. Não restou bloqueio de autorização.
- Falha inicial de preparação: contexto Docker Desktop sem daemon ativo, nenhum container criado; usado explicitamente `docker --context default` (daemon local). Fixture exclusiva em 55574, banco `go033_parity`. Nenhuma falha das suítes mascarada por retry.
- Revisão acrescentou recusa de Python otimizado (`-O`/`PYTHONOPTIMIZE`) para impedir desativação dos asserts de validação. Oito testes do validador PASS, incluindo evidência ausente, skip, zero testes, navegador ausente e proteção da prévia.
- `actionlint` 1.7.7 no novo workflow: exit 0. `bash -n` nos dois scripts: exit 0. Ensaio de porta ocupada com listener real: harness retornou erro antes de seed, listener continuou aceitando conexões (exit 0 do ensaio).
- Nova coleta `/tmp/go033-parity-02` com `--gate integral`: demonstração real da recusa de promoção além dos testes do avaliador. Relatórios anteriores preservados.

### Achados que condicionam promoção

A GO-033 audita o escopo contratado; não implementa silenciosamente todas as lacunas restantes. Todas as 70 capacidades têm resultado: testes executados para cobertura existente e lacunas explícitas para partes ausentes. `PASS` de suíte valida seu corpus; linhas `PARTIAL`/`BLOCKED` não são paridade aprovada. A seleção do piloto é conservadora, ainda sem inventário de um tenant de produção.

- `cmd/server/records.go`: `records.CreateRecord(..., nil)` não conecta os hooks de automação do domínio. Testes de triggers/scheduler/workflow verdes não comprovam automação após escrita HTTP do usuário (CAP-047/048/051/053).
- Views/UI: corpus browser exercita List/editor com sessão semeada. Formulários Edit e outros templates usados por guitars, ponte de persistência completa do canvas e login final continuam pendentes (CAP-001/033/034/038/040).
- Plugins/expressões: host JS real é exercitado; permanece temporário e necessário. Plugins terceiros/auth e substituições da aplicação completa não têm evidência suficiente para corte (CAP-004/042–046/054).
- SQLite: registros/outbox/sync/CLI administrativa têm corpus real, inclusive crash/recovery. Web/identidade/views/worker SQLite não foram validados como produto disponível (CAP-016/023).
- Mobile: cliente JS com SQLite e HTTP real foi executado. Android/iOS, driver Capacitor, build/assinatura e push permanecem sem execução (CAP-055–059/063).
- Instalação/upgrade/restore é do formato Go: schema 1→2 e compatibilidade 1; não substitui ensaio GO-034 sobre origem legada. SLOs/carga/canário dependem de GO-035/036.

Esses limites já constavam das decisões anteriores e foram confrontados com a árvore atual. Nenhuma regressão invalidou testes/dependências integradas; a auditoria impede transformar aceites parciais em declaração de migração completa.

### Validação final — 2026-09-20T11:17:13.754844+00:00

Coleta `/tmp/go033-parity-02`, comando `python3 migracao/parity/run.py --output /tmp/go033-parity-02 --gate integral` na raiz, com as quatro variáveis SALTCORN de teste e confirmação de fixture descartável conforme o README. Exit **1 esperado** do gate integral; `audit_passed=true`, todas as oito suítes PASS, nenhum skip inesperado e inputs estáveis. Não é falha de teste nem aceite de migração completa.

- Inputs SHA-256: `3fd8c410627de59d2a944fd7103240538830aa9e7b10028c8113f5ddd99ecf60`; HEAD base `5eeca039f163fa55563ab32e28dffa2d88e76fdc` + árvore exata identificada em [inputs.json](../paridade/inputs.json). Conferência byte a byte repetida antes do commit.
- [Resultado de cada capacidade](../paridade/RESULTADOS.md) e [relatório estruturado](../paridade/report.json) incluem comandos/cwd/exit/tempo/hash, versões e nomes de testes Go. Logs completos locais preservados; CI publicará nova evidência no SHA do PR.
- Backend: 471 testes/subtestes aprovados com `-race`; únicos skips: benchmark GO-016 e helper de crash executado pelo pai aprovado. HTTP mobile e RLS real incluídos.
- BFF 50, frontend 40, pluginhost 10, cliente SQLite 12, Chromium 6 e Firefox 6 aprovados. Contratos TS/Go sem drift; release Linux real com instalação, upgrade, backup/restore/restart aprovada.
- Auditoria: 70/70 capacidades contabilizadas, sem evidência obrigatória ausente; piloto bloqueado em 39/48 linhas, integral em 60/70. Lacunas explícitas, não dispensas silenciosas.
- Consistência: 38 IDs únicos, estados/checkboxes sincronizados, dependências existentes sem ciclos; só GO-031/032/033 alteradas no CSV.

Revisão técnica local: rastreabilidade, gates e isolamento conferidos; revisão externa pendente. Próximo passo: commit/push e PR único, estado IN_REVIEW somente após publicação. Sem merge/deploy automático.
