# GO-006 — Definir contratos e fixtures HTTP

## Tentativa GO-006-20260916T021252Z-01

- Executor / revisor: Claude Code (agente) / pendente
- Início / última atualização UTC: 2026-09-16T02:12:52Z / 2026-09-16T02:28:33Z
- Estado / motivo: DONE — PR revisado e integrado a `master` pelo autor do repositório
- Branch / base / commit / identificação do diff ou artefato: `task/go-006` / `master` (inclui GO-001..005 integrados) / `migracao/contracts/`
- URL e estado do PR / SHA validado / merge commit quando integrado: https://github.com/vjuliani/saltcorn/pull/9 (MERGED em 2026-09-16T02:32:42Z por vjuliani) / SHA validado `654aa2671afb9cdc2e00e5773f8742967e363f42` / merge commit `ee9fccaaa1e6dbcc942060b0a709dbc8f8d6b325`
- Ambiente / versões / fixture: Go `1.22.2`, Node `v22.23.2`; ferramentas de contrato ainda a definir (lint/codegen)
- Dependências verificadas e evidências: GO-001 DONE (matriz de capacidades — rotas legadas em `packages/server/routes/api.ts`/`scapi.ts` usadas para o mapeamento; riscos de decimal/data/timezone/null/chaves compostas já sinalizados); GO-003 DONE (ADR-0001 define o exemplo `CreateRecord`/`ListRecords` usado como base do contrato; ADR-0003 define a identidade delegada ator+tenant entre BFF e Go)
- Escopo reservado / proprietário de escrita: `migracao/contracts/` (novo), `.github/workflows/migracao-contracts-ci.yml` (novo), `docs/migracao-go/TASKS.md` (seção GO-006), `docs/migracao-go/tasks.csv` (linha GO-006), `docs/migracao-go/execucoes/GO-006.md`
- **Nota de escopo:** os contratos cobrem um recorte representativo (registros dinâmicos via CQRS — Create/List/Get/Update/Delete — e a fronteira de identidade delegada), não a superfície completa do domínio (que ainda não existe em Go). Views/pages/layouts entram quando GO-011/GO-020 existirem; este contrato inicial é o que a fundação (GO-005) e a tenancy (GO-007) precisam para começar.
- Subtarefas concluídas / pendentes:
  - [x] Preflight (dependências GO-001/GO-003 confirmadas DONE, git status, branch, PR existente)
  - [x] Registrar GO-005 como DONE (PR #8 já integrado)
  - [x] Criar branch `task/go-006`
  - [x] `migracao/contracts/openapi/common.yaml`: convenções de erro, paginação, ID, data/hora (UTC explícito), decimal (string), null (JSON Merge Patch), esquema de identidade delegada
  - [x] `migracao/contracts/openapi/internal-api.yaml`: contrato BFF→Go (comandos/queries de registros, idempotência, health/readiness)
  - [x] `migracao/contracts/openapi/bff-api.yaml`: contrato React→BFF (sessão/CSRF, mesmos recursos via envelope de UI)
  - [x] Exemplos positivos e negativos de identidade delegada — `fixtures/identidade-delegada.md` (3 casos worked) + `gen/go/example/client_example_test.go` (os mesmos 3 casos como testes executáveis contra servidor fake)
  - [x] Lint dos contratos com `@redocly/cli` (config `redocly.yaml`) — limpo após corrigir um bug real de YAML (string não citada com `": "` quebrando o parser)
  - [x] Gerar cliente Go a partir de cada contrato (`oapi-codegen`) e confirmar que compila, vetta e passa `go test -race`
  - [x] Gerar cliente TS a partir de cada contrato (`openapi-typescript`) e confirmar que compila/typecheck via um cliente de exemplo real (`gen/ts/client-example.ts`), não só o `.d.ts` isolado
  - [x] `migracao/contracts/mapeamento-apis-legadas.md`: mapeadas as rotas de `api.ts`/`scapi.ts` (GO-001 §2.6) para os novos contratos, com lacunas explícitas (contagem/agregação, emissão de evento, ação de plugin, API pública externa, introspecção)
  - [x] CI: `.github/workflows/migracao-contracts-ci.yml` (lint, geração TS+Go, diff-check de artefatos gerados contra os contratos, build/vet/test do cliente Go), escopado por path
  - [ ] Sincronizar TASKS.md/tasks.csv, commit, push, abrir PR único (em andamento nesta tentativa)

### Validações

| Critério de aceite | Comando/procedimento e diretório | Resultado / exit code | Evidência / versão validada |
| --- | --- | --- | --- |
| Clientes Go/TS validam ambos os contratos | Em `migracao/contracts/`: `npm run lint && npm run gen:ts && npm run typecheck`; `oapi-codegen` + `go build ./... && go vet ./... && go test -race -count=1 ./...` em `gen/go/` | OK — exit 0 em todos; 5 testes Go passando contra servidor fake, typecheck TS limpo com um cliente de exemplo real (não só o `.d.ts`) | saída completa no Checkpoint |
| CI detecta incompatibilidade | `.github/workflows/migracao-contracts-ci.yml`: lint + regeneração de TS/Go a partir dos contratos + `git diff --exit-code` contra o `gen/` commitado (falha se os artefatos gerados divergirem do que está no contrato) | OK — verificado localmente reproduzindo os mesmos passos; nenhuma diferença após regeneração | idem |
| Identidade entre serviços e delegação de ator/tenant têm exemplos positivos e negativos | `fixtures/identidade-delegada.md` (1 positivo + 2 negativos, worked HTTP examples) e os mesmos 3 casos como testes Go executáveis | OK — `TestListFirstPage_Success`, `TestListFirstPage_Unauthorized`, `TestListFirstPage_TenantMismatch` todos PASS | `gen/go/example/client_example_test.go` |

### Checkpoint

- Data/hora UTC e estado consistente comprovado: 2026-09-16T02:28:33Z — contratos, geração e CI completos, todos os checks locais limpos
- Operações persistentes / chaves de idempotência não sensíveis: nenhuma (tarefa documental/contratual, sem código de execução em produção)
- Jobs/processos/leases ainda ativos: nenhum — `npx redocly`/`openapi-typescript`/`oapi-codegen` rodam e terminam, nenhum processo de longa duração
- Divergências, falhas e evidências invalidadas:
  - Bug real de YAML corrigido: `common.yaml` tinha duas descrições em escalar plano contendo `": "` sem aspas (`"(ex.: ..."`), o que quebra o parser YAML por causa da ambiguidade com sintaxe de mapa — corrigido citando as strings, não reescrevendo o conteúdo.
  - `redocly.yaml` precisou ser `redocly.yaml` (sem ponto inicial) para ser descoberto — `.redocly.yaml` não foi reconhecido pela versão instalada.
  - `oapi-codegen/runtime` mais recente (`v1.7.0`+) exige Go ≥1.24; fixado em `v1.2.0` (última versão compatível com Go 1.20+, confirmada via `go.mod` de cada versão no proxy) para não forçar upgrade do toolchain fora do escopo desta tarefa.
  - O padrão `allOf` para o schema `Page` gera uma struct anônima no `oapi-codegen` em vez de reaproveitar o tipo nomeado — contornado no cliente de exemplo (retorna itens/cursor separadamente) e documentado como limitação conhecida no README, não escondido.
- Bloqueio e condição de desbloqueio: nenhum bloqueio
- Próximo passo exato e checks para retomada: nenhum para esta tentativa. GO-007 (tenancy) e GO-008 (identidade) são os consumidores naturais — GO-008 implementa a validação real do `ServiceIdentity` cujo comportamento está especificado aqui e em `fixtures/identidade-delegada.md`
- Recuperação/rollback aplicável: não aplicável (nenhuma mutação de dados)
- Revisão / decisão de encerramento: PR #9 revisado e integrado a `master` por vjuliani em 2026-09-16T02:32:42Z (merge `ee9fccaaa1e6dbcc942060b0a709dbc8f8d6b325`); tarefa encerrada como DONE

#### Evidência: validação local completa

```
$ cd migracao/contracts
$ npm run lint
✅ internal-api.yaml e bff-api.yaml validados (redocly, config em redocly.yaml)

$ npm run gen:ts && npm run typecheck
🚀 internal-api → gen/ts/internal-api.d.ts
🚀 bff-api → gen/ts/bff-api.d.ts
(tsc --noEmit sem erros, incluindo gen/ts/client-example.ts)

$ npm run bundle:internal && npm run bundle:bff
$ oapi-codegen -config oapi-codegen.internal-api.yaml gen/bundled/internal-api.json
$ oapi-codegen -config oapi-codegen.bff-api.yaml gen/bundled/bff-api.json
$ cd gen/go && go build ./... && go vet ./... && gofmt -l .
(sem saída — build/vet/format limpos)
$ go test -race -count=1 ./...
ok  .../gen/go/example  (5 testes: sucesso, 401 identidade forjada, 403 tenant
                          divergente, criação de registro, bootstrap do BFF)
```
