# GO-006 — Definir contratos e fixtures HTTP

## Tentativa GO-006-20260916T021252Z-01

- Executor / revisor: Claude Code (agente) / pendente
- Início / última atualização UTC: 2026-09-16T02:12:52Z
- Estado / motivo: IN_PROGRESS — preflight concluído (incl. registro de GO-005 como DONE), desenhando os contratos
- Branch / base / commit / identificação do diff ou artefato: `task/go-006` / `master` (inclui GO-001..005 integrados) / pendente
- URL e estado do PR / SHA validado / merge commit quando integrado: pendente
- Ambiente / versões / fixture: Go `1.22.2`, Node `v22.23.2`; ferramentas de contrato ainda a definir (lint/codegen)
- Dependências verificadas e evidências: GO-001 DONE (matriz de capacidades — rotas legadas em `packages/server/routes/api.ts`/`scapi.ts` usadas para o mapeamento; riscos de decimal/data/timezone/null/chaves compostas já sinalizados); GO-003 DONE (ADR-0001 define o exemplo `CreateRecord`/`ListRecords` usado como base do contrato; ADR-0003 define a identidade delegada ator+tenant entre BFF e Go)
- Escopo reservado / proprietário de escrita: `migracao/contracts/` (novo), `.github/workflows/migracao-contracts-ci.yml` (novo), `docs/migracao-go/TASKS.md` (seção GO-006), `docs/migracao-go/tasks.csv` (linha GO-006), `docs/migracao-go/execucoes/GO-006.md`
- **Nota de escopo:** os contratos cobrem um recorte representativo (registros dinâmicos via CQRS — Create/List/Get/Update/Delete — e a fronteira de identidade delegada), não a superfície completa do domínio (que ainda não existe em Go). Views/pages/layouts entram quando GO-011/GO-020 existirem; este contrato inicial é o que a fundação (GO-005) e a tenancy (GO-007) precisam para começar.
- Subtarefas concluídas / pendentes:
  - [x] Preflight (dependências GO-001/GO-003 confirmadas DONE, git status, branch, PR existente)
  - [x] Registrar GO-005 como DONE (PR #8 já integrado)
  - [x] Criar branch `task/go-006`
  - [ ] `migracao/contracts/openapi/common.yaml`: convenções de erro, paginação, ID, data/hora (UTC explícito), decimal (string), null (JSON Merge Patch), esquema de identidade delegada
  - [ ] `migracao/contracts/openapi/internal-api.yaml`: contrato BFF→Go (comandos/queries de registros, idempotência, health/readiness)
  - [ ] `migracao/contracts/openapi/bff-api.yaml`: contrato React→BFF (sessão/CSRF, mesmos recursos via envelope de UI)
  - [ ] Exemplos positivos e negativos de identidade delegada (token válido; assinatura inválida; tenant divergente) nos dois contratos onde aplicável
  - [ ] Lint dos contratos (detectar incompatibilidade) — ferramenta a definir
  - [ ] Gerar cliente Go a partir de cada contrato e confirmar que compila
  - [ ] Gerar cliente TS a partir de cada contrato e confirmar que compila/typecheck
  - [ ] `migracao/contracts/mapeamento-apis-legadas.md`: mapear rotas legadas (`api.ts`/`scapi.ts`, GO-001 §2.6) para os novos contratos, com lacunas explícitas
  - [ ] CI: `.github/workflows/migracao-contracts-ci.yml` (lint + geração de clientes), escopado por path
  - [ ] Sincronizar TASKS.md/tasks.csv, commit, push, abrir PR único

### Validações

| Critério de aceite | Comando/procedimento e diretório | Resultado / exit code | Evidência / versão validada |
| --- | --- | --- | --- |
| Clientes Go/TS validam ambos os contratos | Gerar cliente Go e cliente TS a partir de `internal-api.yaml` e de `bff-api.yaml`; confirmar build/typecheck limpo | Pendente | a preencher |
| CI detecta incompatibilidade | Lint automatizado dos contratos rodando em CI, escopado por path | Pendente | idem |
| Identidade entre serviços e delegação de ator/tenant têm exemplos positivos e negativos | `components.examples` nos contratos: token válido, assinatura inválida, tenant divergente | Pendente | idem |

### Checkpoint

- Data/hora UTC e estado consistente comprovado: 2026-09-16T02:12:52Z — branch criada, registro de GO-005 já commitado, contratos ainda não escritos
- Operações persistentes / chaves de idempotência não sensíveis: nenhuma (tarefa documental/contratual, sem código de execução em produção)
- Jobs/processos/leases ainda ativos: nenhum
- Divergências, falhas e evidências invalidadas: nenhuma
- Bloqueio e condição de desbloqueio: nenhum bloqueio no momento
- Próximo passo exato e checks para retomada: escrever `common.yaml`, depois `internal-api.yaml` e `bff-api.yaml`, então validar com lint e codegen
- Recuperação/rollback aplicável: não aplicável (nenhuma mutação de dados)
- Revisão / decisão de encerramento: pendente
