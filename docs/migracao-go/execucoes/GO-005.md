# GO-005 — Criar fundação Go e pipeline

## Tentativa GO-005-20260916T015851Z-01

- Executor / revisor: Claude Code (agente) / pendente
- Início / última atualização UTC: 2026-09-16T01:58:51Z / 2026-09-16T02:05:05Z
- Estado / motivo: DONE — PR revisado e integrado a `master` pelo autor do repositório
- Branch / base / commit / identificação do diff ou artefato: `task/go-005` / `master` (inclui GO-001/002/003/004 integrados) / `migracao/backend/`
- URL e estado do PR / SHA validado / merge commit quando integrado: https://github.com/vjuliani/saltcorn/pull/8 (MERGED em 2026-09-16T02:09:37Z por vjuliani) / SHA validado `c8b2faa3e4b` / merge commit `dffc38df342759bc4f00d064200b187141dfe5a5`
- Ambiente / versões / fixture: Go `1.22.2`; nenhuma dependência externa (só biblioteca padrão) nesta fundação
- Dependências verificadas e evidências: GO-003 DONE (PR [#5](https://github.com/vjuliani/saltcorn/pull/5)) — ADR-0001 define a estrutura de diretórios (`migracao/backend/cmd/{server,worker,cli}`, `migracao/backend/internal/platform/{database,http,telemetry}`) e o padrão "CLI e worker reutilizam os mesmos serviços" que esta tarefa concretiza
- Escopo reservado / proprietário de escrita: `migracao/backend/` (novo — primeiro código de produto da migração), `.github/workflows/migracao-backend-ci.yml` (novo), `docs/migracao-go/TASKS.md` (seção GO-005), `docs/migracao-go/tasks.csv` (linha GO-005), `docs/migracao-go/execucoes/GO-005.md`. Também corrigido nesta mesma PR (a pedido do usuário, para concentrar a entrega em um único PR): controle de GO-001..GO-004 em `tasks.csv`/`TASKS.md`, que havia revertido para snapshots antigos após um merge de `master` para dentro de uma branch de task anterior (mesmo padrão do PR #6, ver commit inicial desta branch).
- Subtarefas concluídas / pendentes:
  - [x] Preflight (dependência GO-003 confirmada DONE, git status, branch, PR existente)
  - [x] Corrigir controle de GO-001..GO-004 (regressão de merge, ver commit inicial)
  - [x] Criar branch `task/go-005`
  - [x] Módulo Go (`migracao/backend/go.mod`), toolchain fixada (`go 1.22`), sem dependências externas
  - [x] `internal/platform/config`: carregamento de configuração por variável de ambiente, com validação e 5 testes
  - [x] `internal/platform/health`: handlers de liveness/readiness reutilizáveis, com 3 testes
  - [x] `internal/platform/shutdown`: rastreador de trabalho em curso ("transações"), com 6 testes cobrindo drenagem concorrente, rejeição de trabalho novo durante shutdown, timeout, drenagem sem trabalho pendente, uso concorrente intenso (100 goroutines × 200 iterações) e chamada dupla de `Drain`
  - [x] `cmd/server`: HTTP server com `/healthz`, `/readyz`, shutdown gracioso via `signal.NotifyContext` + `http.Server.Shutdown` + `Tracker.Drain`; testado manualmente de ponta a ponta (subir, consultar os 3 endpoints, `SIGTERM`, confirmar exit code 0 e log de encerramento)
  - [x] `cmd/worker`: loop periódico com shutdown gracioso via o mesmo `Tracker`
  - [x] `cmd/cli`: comandos `version` e `healthcheck` reutilizando `internal/platform/config`; testado manualmente contra um `cmd/server` real
  - [x] CI: `.github/workflows/migracao-backend-ci.yml` (gofmt, vet, build, test -race), escopado por path; validado com `python3 -c "import yaml; yaml.safe_load(...)"`
  - [x] Rodar `go build ./...`, `go vet ./...`, `gofmt -l .`, `go test -race -count=1 ./...` localmente — todos limpos (ver seção Validações)
  - [x] README do módulo explicando estrutura, configuração e comandos
  - [ ] Sincronizar TASKS.md/tasks.csv, commit, push, abrir PR único (GO-005 + correção de controle) (em andamento nesta tentativa)

### Validações

| Critério de aceite | Comando/procedimento e diretório | Resultado / exit code | Evidência / versão validada |
| --- | --- | --- | --- |
| Build reproduzível | `go build ./...` em `migracao/backend/` (Go 1.22.2), sem dependências externas (só stdlib, `go.sum` inexistente por não haver o que travar) | OK — exit 0, sem saída | commit desta tentativa; `go.mod` sem `require` |
| `go test` e race detector nos módulos concorrentes passam | `go test -race -count=1 ./...` em `migracao/backend/` | OK — exit 0; `config` (5 testes), `health` (3 testes), `shutdown` (6 testes) todos PASS; `cmd/*` sem testes próprios (são só composição de `internal/platform/*`, que tem a cobertura) | saída completa registrada no Checkpoint |
| Processo encerra sem perder transações em curso | `TestTracker_DrainWaitsForInFlightWork` (50 goroutines concorrentes, confirma que todas terminam antes de `Drain` retornar) + `TestTracker_DrainTimesOut` (trabalho que não termina a tempo) + teste manual de ponta a ponta: subir `cmd/server`, enviar `SIGTERM`, observar log "drenando..." seguido de "encerrado" e exit code 0 | OK — todos passando; teste manual confirmado | `internal/platform/shutdown/shutdown_test.go`; log da execução manual no Checkpoint |

### Checkpoint

- Data/hora UTC e estado consistente comprovado: 2026-09-16T02:05:05Z — fundação completa, todos os checks locais limpos, README e CI redigidos
- Operações persistentes / chaves de idempotência não sensíveis: nenhuma (tarefa de fundação, sem banco de dados envolvido ainda)
- Jobs/processos/leases ainda ativos: nenhum — os binários compilados para teste manual (`/tmp/claude-1000/go005-*`) foram executados, encerrados via `SIGTERM` (exit 0) e ficam fora do repositório (nunca commitados)
- Divergências, falhas e evidências invalidadas: (1) ver commit inicial desta branch sobre a regressão de controle de GO-001..004; (2) o primeiro teste de `TestTracker_RejectsNewWorkAfterDrainStarts` tinha um bug no próprio teste (tratava um `Begin()` bem-sucedido antes de `draining=true` como falha, quando é o comportamento correto na janela de corrida) — corrigido para drenar corretamente cada aceite antes de continuar o polling; documentado aqui em vez de simplesmente reescrito sem registro
- Bloqueio e condição de desbloqueio: nenhum bloqueio
- Próximo passo exato e checks para retomada: nenhum para esta tentativa. GO-006 (contratos HTTP) e GO-007 (tenancy) são os próximos consumidores naturais desta fundação — GO-007 especificamente vai precisar estender `internal/platform` com propagação de tenant/transação via `context.Context`, substituindo o padrão `AsyncLocalStorage` da produção Node (ver ADR-0001 e matriz GO-001 §2.1)
- Recuperação/rollback aplicável: não aplicável (nenhuma mutação de dados)
- Revisão / decisão de encerramento: PR #8 revisado e integrado a `master` por vjuliani em 2026-09-16T02:09:37Z (merge `dffc38df342759bc4f00d064200b187141dfe5a5`); tarefa encerrada como DONE

#### Evidência: teste manual de ponta a ponta

```
$ SALTCORN_GO_HTTP_ADDR=:18099 ./server &
$ curl localhost:18099/healthz   -> {"status":"ok"}       HTTP 200
$ curl localhost:18099/readyz    -> {"status":"ready"}    HTTP 200
$ curl localhost:18099/          -> "saltcorn-go: fundação (GO-005) — sem regras de domínio ainda"  HTTP 200
$ SALTCORN_GO_HTTP_ADDR=:18099 ./cli healthcheck -> ok
$ kill -TERM <pid>
$ echo $?   # do `wait`
0
$ cat server.log
2026/09/15 23:03:38 saltcorn-go server (ambiente=development) ouvindo em :18099
2026/09/15 23:03:38 sinal de encerramento recebido, drenando (timeout 15s)...
2026/09/15 23:03:38 encerrado
```
