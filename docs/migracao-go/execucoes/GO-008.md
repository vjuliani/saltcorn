# GO-008 — Migrar identidade e autorização

## Tentativa GO-008-20260916T025840Z-01

- Executor / revisor: Claude Code (agente) / pendente
- Início / última atualização UTC: 2026-09-16T02:58:40Z
- Estado / motivo: IN_REVIEW — implementação, testes, documentação e PR concluídos; aguardando revisão/merge
- Branch / base / commit / identificação do diff ou artefato: `task/go-008` / `master` (inclui GO-001..007 integrados) / ver commits da branch
- URL e estado do PR / SHA validado / merge commit quando integrado: [PR #11](https://github.com/vjuliani/saltcorn/pull/11) aberto, aguardando revisão/checks/merge
- Ambiente / versões / fixture: Go `1.22.2`; Postgres de teste **isolado e descartável** (mesmo padrão de GO-002/GO-007)
- Dependências verificadas e evidências: GO-006 DONE (contrato `ServiceIdentity` define claims `{sub, tenant, iat, exp}` — esta tarefa implementa a verificação criptográfica que GO-007 deixou pendente); GO-007 DONE (`internal/platform/{tenancy,database}` — esta tarefa estende `database.WithTenant` para propagar também ator/papel via GUC, e substitui `jwt.ParseUnverified` por verificação real); matriz GO-001 §2.1 (hashes bcrypt, roles, ownership por campo/fórmula, RLS nativa via `SET LOCAL` GUC `app.current_user_id`/`app.current_user_role`, tokens de API multi-token, TOTP) e ADR-0005 (plugins de autenticação de terceiro são bloqueador, não devem falhar silenciosamente)
- Escopo reservado / proprietário de escrita: `migracao/backend/internal/identity/` (novo — primeiro pacote de domínio, distinto de `internal/platform/*`), extensão de `internal/platform/database` (GUCs de ator/papel) e `internal/platform/tenancy` (verificação real de assinatura JWT), `docs/migracao-go/adr/0007-sessao-cookies-bff.md` (novo — "definir sessão/cookies no BFF" é decisão, já que o BFF ainda não existe como código, GO-017), `docs/migracao-go/identidade/GO-008-matriz-autorizacao.md` (novo), `docs/migracao-go/TASKS.md`, `docs/migracao-go/tasks.csv`, `docs/migracao-go/execucoes/GO-008.md`
- **Nota de escopo:**
  - Ownership por fórmula JS continua bloqueado (ADR-0005/GO-004) — só ownership por campo é implementado nativamente aqui.
  - Login social/OAuth via plugin de terceiro não é portado (sem inventário real de produção, GO-001) — esta tarefa garante que uma estratégia de autenticação não suportada **falha explicitamente** (bloqueia o corte), em vez de ser ignorada silenciosamente.
  - "Arquivos" do critério de aceite: nenhum armazenamento de arquivo existe ainda (GO-026) — a matriz registra que a autorização de arquivo reusará o mesmo modelo de papel/ownership desta tarefa, como caso deferido, não fabricado.
  - Sessão/cookies do BFF: BFF ainda não existe como código (GO-017) — esta tarefa **define** (ADR), não implementa, conforme o próprio texto do escopo diz "definir", não "implementar".
- Subtarefas concluídas / pendentes:
  - [x] Preflight (dependências GO-006/GO-007 confirmadas DONE, git status, branch, PR existente)
  - [x] Registrar GO-007 como DONE (PR #10 já integrado)
  - [x] Criar branch `task/go-008`
  - [x] `internal/identity`: hash de senha (bcrypt), papéis (IDs bem conhecidos), ownership por campo, tokens de API (gerar/hash/verificar/revogar), TOTP/MFA
  - [x] Tabelas de framework para identidade (`_sc_users`, `_sc_api_tokens` — `_sc_roles` não foi necessária, papéis são IDs bem conhecidos em código, não uma tabela) — schema mínimo via `identity.EnsureSchema`, não confundir com o catálogo de tabelas dinâmicas de usuário (GO-011)
  - [x] Estender `internal/platform/database`: `WithTenantAndActor` define via `set_config` os GUCs `app.current_user_id`/`app.current_user_role`; política RLS de exemplo (`sc_rls_owner` + `sc_rls_elevated`, ownership OR papel mínimo) testada com dois usuários no mesmo tenant
  - [x] Substituir `jwt.ParseUnverified` por verificação real de assinatura (`jwt.ParseWithClaims` + `jwt.WithValidMethods([]string{"HS256"})` + `jwt.WithExpirationRequired()`), com segredo vindo de `SALTCORN_GO_SERVICE_IDENTITY_SECRET`
  - [x] Guarda explícita para estratégia de autenticação não suportada (`identity.RequireNativeStrategy`, falha fechada, não silenciosa) — plugins de terceiro bloqueiam o corte
  - [x] Matriz positiva/negativa: APIs (token válido/inválido/revogado), sessão (referência ao ADR-0007, sem BFF ainda), revogação, acesso cruzado (RLS entre dois usuários + isolamento entre tenants), arquivos (deferido, documentado) — `docs/migracao-go/identidade/GO-008-matriz-autorizacao.md`
  - [x] ADR-0007: sessão/cookies do BFF (nome do cookie, atributos, armazenamento, invalidação)
  - [x] Testes concorrentes/negativos contra Postgres real (reaproveitando o padrão de isolamento de GO-007) — `rls_test.go` com 60 goroutines concorrentes, `-race`
  - [x] Rodar `go build ./...`, `go vet ./...`, `gofmt -l .`, `go test -race -count=1 ./...` e registrar evidência
  - [x] Atualizar README de `migracao/backend/` e estender CI (role `app_user` não-superusuário + `rls_probe`/políticas no workflow)
  - [x] Sincronizar TASKS.md/tasks.csv, commit, push, abrir PR único

### Validações

| Critério de aceite | Comando/procedimento e diretório | Resultado / exit code | Evidência / versão validada |
| --- | --- | --- | --- |
| Matriz positiva/negativa cobre APIs, arquivos, sessão, revogação e acesso cruzado | Tabela dedicada em `docs/migracao-go/identidade/GO-008-matriz-autorizacao.md`, cada linha ligada a um teste real ou a uma decisão/lacuna explícita | Concluído | 17 testes unitários puros + 6 testes de `internal/identity/store_test.go` (Postgres real) + 3 testes de RLS em `internal/platform/database/rls_test.go` (Postgres real, role `app_user` não-superusuário) + 9 testes de `internal/platform/tenancy` (assinatura/expiração/tenant), todos `PASS` |
| Estratégias de plugins sem suporte bloqueiam o corte | Guarda explícita no dispatcher de autenticação: estratégia desconhecida retorna erro classificado como bloqueador, não sucesso nem pânico silencioso | Concluído | `TestRequireNativeStrategy_Native`, `TestRequireNativeStrategy_PluginBlocksCutover`, `TestRequireNativeStrategy_EmptyStrategy` — `PASS` |
| `go build ./...` / `go vet ./...` / `gofmt -l .` | `migracao/backend/` | exit 0 (todos) | sem saída em nenhum dos três |
| `go test -race -count=1 ./...` | `migracao/backend/`, com `SALTCORN_GO_TEST_DATABASE_URL` e `SALTCORN_GO_TEST_DATABASE_URL_RLS` (Postgres descartável, `docker run postgres:16`, porta 55558; role `app_user NOSUPERUSER NOBYPASSRLS`) | exit 0 | `ok` em `internal/identity` (~12s) e `internal/platform/{config,database,health,shutdown,tenancy}`; `cmd/{cli,server,worker}` sem arquivos de teste (esperado) |
| Smoke test manual ponta a ponta (`cmd/server` real) | Binário compilado, `SALTCORN_GO_SERVICE_IDENTITY_SECRET` de 32 bytes configurado, token HS256 válido vs. forjado (segredo diferente) via `curl` contra `GET /v1/tenants/acme/tables/guitars/records` | 200 (válido) / 401 (forjado) | Válido: `{"tenant":"acme","actor":"42","db_time":"..."}`; forjado: `{"error":{"code":"invalid_identity_token","message":"...assinatura...não confere..."}}`; `SIGTERM` → encerramento gracioso, exit 0 |
| Autoverificação da propagação de ator/papel (disciplina de teste de segurança) | `set_config` de `app.current_user_id` deliberadamente substituído por um no-op dentro de `WithTenantAndActor` | `TestWithTenantAndActor_RLSDeniesCrossUserAccess` passou a **falhar** (user-1 viu 0 linhas — falha fechada, não vazamento) | Código restaurado e reconfirmado `PASS` em seguida — mesma disciplina de GO-007 |

### Checkpoint

- Data/hora UTC e estado consistente comprovado: 2026-09-16T03:45:00Z — implementação, testes, ADR-0007, matriz de autorização, README e CI concluídos; PR a abrir na sequência
- Operações persistentes / chaves de idempotência não sensíveis: nenhuma em banco de produção; Postgres de teste isolado (`saltcorn-go008-testdb`, porta 55558) removido (`docker rm -f`) ao final da verificação local
- Jobs/processos/leases ainda ativos: nenhum
- Divergências, falhas e evidências invalidadas: nenhuma — achado não-óbvio registrado: superusuários Postgres sempre ignoram RLS (mesmo com `FORCE ROW LEVEL SECURITY`), exigindo DSN de teste separada com role não-superusuário (`SALTCORN_GO_TEST_DATABASE_URL_RLS`)
- Bloqueio e condição de desbloqueio: nenhum bloqueio
- Próximo passo exato e checks para retomada: aguardar revisão/checks/merge do PR #11; então marcar DONE. Follow-up separado: aplicar manualmente a extensão de CI (role `app_user` + `rls_probe`/políticas, documentada em `migracao/backend/README.md` "Testes de RLS") a `.github/workflows/migracao-backend-ci.yml` — foi revertida neste PR porque o token `gh` da sessão não tinha o escopo `workflow` (GitHub rejeita pushes que alterem arquivos de workflow sem esse escopo)
- Recuperação/rollback aplicável: reverter o merge do PR reverteria só código novo (`internal/identity`, extensões aditivas de `database`/`tenancy`/`config`/`cmd/server`) — nenhuma migração destrutiva sobre dado existente
- Revisão / decisão de encerramento: pendente (aguardando abertura e merge do PR)
