# GO-008 — Migrar identidade e autorização

## Tentativa GO-008-20260916T025840Z-01

- Executor / revisor: Claude Code (agente) / pendente
- Início / última atualização UTC: 2026-09-16T02:58:40Z
- Estado / motivo: IN_PROGRESS — preflight concluído (incl. registro de GO-007 como DONE), desenhando identidade/autorização
- Branch / base / commit / identificação do diff ou artefato: `task/go-008` / `master` (inclui GO-001..007 integrados) / pendente
- URL e estado do PR / SHA validado / merge commit quando integrado: pendente
- Ambiente / versões / fixture: Go `1.22.2`; Postgres de teste **isolado e descartável** (mesmo padrão de GO-002/GO-007)
- Dependências verificadas e evidências: GO-006 DONE (contrato `ServiceIdentity` define claims `{sub, tenant, iat, exp}` — esta tarefa implementa a verificação criptográfica que GO-007 deixou pendente); GO-007 DONE (`internal/platform/{tenancy,database}` — esta tarefa estende `database.WithTenant` para propagar também ator/papel via GUC, e substitui `jwt.ParseUnverified` por verificação real); matriz GO-001 §2.1 (hashes bcrypt, roles, ownership por campo/fórmula, RLS nativa via `SET LOCAL` GUC `app.current_user_id`/`app.current_user_role`, tokens de API multi-token, TOTP) e ADR-0005 (plugins de autenticação de terceiro são bloqueador, não devem falhar silenciosamente)
- Escopo reservado / proprietário de escrita: `migracao/backend/internal/identity/` (novo — primeiro pacote de domínio, distinto de `internal/platform/*`), extensão de `internal/platform/database` (GUCs de ator/papel) e `internal/platform/tenancy` (verificação real de assinatura JWT), `docs/migracao-go/adr/0007-sessao-cookies-bff.md` (novo — "definir sessão/cookies no BFF" é decisão, já que o BFF ainda não existe como código, GO-017), `docs/migracao-go/identidade/GO-008-matriz-autorizacao.md` (novo), `docs/migracao-go/TASKS.md`, `docs/migracao-go/tasks.csv`, `docs/migracao-go/execucoes/GO-008.md`
- **Nota de escopo:**
  - Ownership por fórmula JS continua bloqueado (ADR-0005/GO-004) — só ownership por campo é implementado nativamente aqui.
  - Login social/OAuth via plugin de terceiro não é portado (sem inventário real de produção, GO-001) — esta tarefa garante que uma estratégia de autenticação não suportada **falha explicitamente** (bloqueia o corte), em vez de ser ignorada silenciosamente.
  - "Arquivos" do critério de aceite: nenhum armazenamento de arquivo existe ainda (GO-026) — a matriz registra que a autorização de arquivo reusará o mesmo modelo de papel/ownership desta tarefa, como caso deferido, não fabricado.
  - Sessão/cookies do BFF: BFF ainda não existe como código (GO-017) — esta tarefa **define** (ADR), não implementa, conforme o próprio texto do escopo diz "definir", não "implementar".
- Subtarefas concluídas / pendentes:
  - [x] Preflight (dependências GO-006/GO-007 confirmadas DONE, git status, branch, PR existente)
  - [x] Registrar GO-007 como DONE (PR #10 já integrado)
  - [x] Criar branch `task/go-008`
  - [ ] `internal/identity`: hash de senha (bcrypt), papéis (IDs bem conhecidos), ownership por campo, tokens de API (gerar/hash/verificar/revogar), TOTP/MFA
  - [ ] Tabelas de framework para identidade (`_sc_users`, `_sc_roles`, `_sc_api_tokens`) — schema mínimo, migrations simples, não confundir com o catálogo de tabelas dinâmicas de usuário (GO-011)
  - [ ] Estender `internal/platform/database`: `WithTenantAndActor` define também `SET LOCAL app.current_user_id`/`app.current_user_role`; política RLS de exemplo (ownership + min_role) testada com dois usuários no mesmo tenant
  - [ ] Substituir `jwt.ParseUnverified` por verificação real de assinatura (`jwt.ParseWithClaims` + `jwt.WithValidMethods`), com segredo compartilhado vindo de configuração
  - [ ] Guarda explícita para estratégia de autenticação não suportada (falha fechada, não silenciosa) — plugins de terceiro bloqueiam o corte
  - [ ] Matriz positiva/negativa: APIs (token válido/inválido/revogado), sessão (referência ao ADR-0007, sem BFF ainda), revogação, acesso cruzado (RLS entre dois usuários), arquivos (deferido, documentado)
  - [ ] ADR-0007: sessão/cookies do BFF (nome do cookie, atributos, armazenamento, invalidação)
  - [ ] Testes concorrentes/negativos contra Postgres real (reaproveitando o padrão de isolamento de GO-007)
  - [ ] Rodar `go build ./...`, `go vet ./...`, `gofmt -l .`, `go test -race -count=1 ./...` e registrar evidência
  - [ ] Atualizar README de `migracao/backend/` e estender CI se necessário
  - [ ] Sincronizar TASKS.md/tasks.csv, commit, push, abrir PR único

### Validações

| Critério de aceite | Comando/procedimento e diretório | Resultado / exit code | Evidência / versão validada |
| --- | --- | --- | --- |
| Matriz positiva/negativa cobre APIs, arquivos, sessão, revogação e acesso cruzado | Tabela dedicada em `docs/migracao-go/identidade/GO-008-matriz-autorizacao.md`, cada linha ligada a um teste real ou a uma decisão/lacuna explícita | Pendente | a preencher |
| Estratégias de plugins sem suporte bloqueiam o corte | Guarda explícita no dispatcher de autenticação: estratégia desconhecida retorna erro classificado como bloqueador, não sucesso nem pânico silencioso | Pendente | idem |

### Checkpoint

- Data/hora UTC e estado consistente comprovado: 2026-09-16T02:58:40Z — branch criada, registro de GO-007 já commitado, implementação ainda não escrita
- Operações persistentes / chaves de idempotência não sensíveis: nenhuma em banco de produção; Postgres de teste isolado e descartável
- Jobs/processos/leases ainda ativos: nenhum
- Divergências, falhas e evidências invalidadas: nenhuma
- Bloqueio e condição de desbloqueio: nenhum bloqueio no momento
- Próximo passo exato e checks para retomada: escrever `internal/identity`, estender `database`/`tenancy`, então a matriz e o ADR
- Recuperação/rollback aplicável: não aplicável ainda (nenhuma mutação além do ambiente de teste descartável)
- Revisão / decisão de encerramento: pendente
