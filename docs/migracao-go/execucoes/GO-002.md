# GO-002 — Criar baseline de comportamento e desempenho

## Tentativa GO-002-20260916T001946Z-01

- Executor / revisor: Claude Code (agente) / pendente
- Início / última atualização UTC: 2026-09-16T00:19:46Z / 2026-09-16T00:59:39Z
- Estado / motivo: IN_REVIEW — baseline consolidado, artefato revisável produzido, PR aberto para revisão humana
- Branch / base / commit / identificação do diff ou artefato: `task/go-002` / `master` (commit `6557f7624104bb7d688b40a33f88ccbe2df5d153`, inclui GO-001 integrado) / `docs/migracao-go/baseline/GO-002-baseline.md`
- URL e estado do PR / SHA validado / merge commit quando integrado: registrar após `gh pr create`
- Ambiente / versões / fixture: Node `v22.23.2`, npm `12.0.2`, Docker `25.0.2`; Postgres de teste **isolado e descartável** em container Docker `saltcorn-go002-testdb` (imagem `postgres:16`, porta `127.0.0.1:55555`, banco `saltcorn_test`) — não usa o serviço Postgres local do host (porta 5432, pertence a outro projeto) nem os containers Docker de outros projetos já em execução na máquina (`clazzera-local-*`, `financas-web-1`). Fixtures: `packages/saltcorn-data/db/fixtures.ts` (sintéticas, sem dado real).
- Dependências verificadas e evidências: GO-001 DONE (PR [#1](https://github.com/vjuliani/saltcorn/pull/1), merge `6557f7624104bb7d688b40a33f88ccbe2df5d153`); matriz de capacidades e escolha de piloto (`guitars`) usadas para orientar quais fixtures/pacotes priorizar nesta baseline.
- Escopo reservado / proprietário de escrita: `docs/migracao-go/baseline/` (novo), `docs/migracao-go/TASKS.md` (seção GO-002), `docs/migracao-go/tasks.csv` (linha GO-002), `docs/migracao-go/execucoes/GO-002.md`. Nenhuma alteração de código de produto.
- Subtarefas concluídas / pendentes (tarefa **L**, dividida conforme protocolo):
  - [x] Preflight (dependência GO-001 confirmada DONE, git status, branch, PR existente)
  - [x] Criar branch `task/go-002`
  - [x] Preparar ambiente de teste isolado (container Postgres descartável, sem tocar infraestrutura de outros projetos na máquina)
  - [x] `npm install --legacy-peer-deps` na raiz (conforme `deploy/docker-compose-tests/*/Dockerfile.test`, já que `npm ci` falhou por lockfile fora de sincronia — ver Checkpoint)
  - [x] `npm run tsc` (build completo do monorepo, pré-requisito de `saltcorn run-tests`)
  - [x] Corrigir bloqueio de `install-scripts` do npm para `sqlite3` (`npm install-scripts approve sqlite3` + reinstalar) — sem isso a descoberta de comandos do `saltcorn-cli` falha e `run-tests` não é encontrado; incluído em `package.json` por ser necessário para qualquer reprodução com npm moderno
  - [x] Rodar `saltcorn run-tests saltcorn-data` contra o Postgres isolado — 846/846 com `TZ=UTC` (55,6s); 2 falhas de timezone sem `TZ=UTC`, reproduzidas e explicadas
  - [x] Rodar `saltcorn run-tests server` — 599/599 (105,5s)
  - [x] Rodar `npm run bench:render` (benchmark de renderização de List view) — 4 estratégias medidas
  - [x] Avaliar viabilidade de Playwright (`deploy/playwright`) — viável após instalar dependências próprias do diretório (não é workspace) e o binário do Chromium; 260/271 passaram (16,1 min), 11 falhas em ~6 causas-raiz (ver relatório)
  - [x] Documentar dataset (fixtures sintéticas já usadas pelas suítes; pack `guitars` registrado como candidato para carga futura, não exercitado nesta rodada)
  - [x] Classificar falhas observadas: defeito conhecido vs. incompatibilidade a resolver na migração (ver relatório §5, §7, §8)
  - [x] Definir limites provisórios de p95/p99, RPO/RTO como hipótese (não SLO comprovado, conforme README §5)
  - [x] Consolidar relatório de baseline em `docs/migracao-go/baseline/GO-002-baseline.md`
  - [x] Limpar ambiente descartável (`docker rm -f saltcorn-go002-testdb`; reverter artefato de teste `deploy/playwright/emailData.json`; não commitar reescrita de `package-lock.json`)
  - [ ] Sincronizar TASKS.md/tasks.csv, commit, push, abrir PR (em andamento nesta tentativa)

### Validações

| Critério de aceite | Comando/procedimento e diretório | Resultado / exit code | Evidência / versão validada |
| --- | --- | --- | --- |
| Execução reproduzível no commit de referência | `saltcorn run-tests saltcorn-data` / `saltcorn run-tests server` / `deploy/playwright/run.sh` contra Postgres isolado, raiz do checkout, commit `6557f7624104bb7d688b40a33f88ccbe2df5d153` | OK — 846/846, 599/599, 260/271 (exit codes 0, 0, 1 respectivamente; o `1` do Playwright é o resultado real de 11 falhas, não uma falha de execução) | `docs/migracao-go/baseline/GO-002-baseline.md` §2–5 |
| Limites p95/p99, RPO/RTO e casos críticos definidos | Medição via `npm run bench:render`; observação de duração das suítes; RPO/RTO e p95/p99 sob carga registrados como hipótese/lacuna explícita, não medição fabricada | OK — parcial por natureza (README já trata como hipótese a refinar em F0); lacunas listadas explicitamente | `docs/migracao-go/baseline/GO-002-baseline.md` §4, §6, §7 |
| Defeitos conhecidos separados da compatibilidade desejada | Classificação manual de cada falha de teste observada, incluindo agrupamento de cascatas Playwright por causa-raiz | OK — 0 defeitos reais em `data`/`server` (2 falhas eram timezone de ambiente, reproduzidas e explicadas); ~6 causas-raiz Playwright registradas como pendência de triagem, não fechadas como defeito confirmado sem uma segunda execução | `docs/migracao-go/baseline/GO-002-baseline.md` §5, §8 |

### Checkpoint

- Data/hora UTC e estado consistente comprovado: 2026-09-16T00:59:39Z — baseline consolidado em `docs/migracao-go/baseline/GO-002-baseline.md`; ambiente descartável removido; TASKS.md/tasks.csv sincronizados nesta mesma alteração
- Operações persistentes / chaves de idempotência não sensíveis: nenhuma em banco de produção; container `saltcorn-go002-testdb` foi removido (`docker rm -f`) ao final da tarefa
- Jobs/processos/leases ainda ativos: nenhum — servidor de teste (`saltcorn serve -p 3014`) foi encerrado pelo próprio `run.sh` (trap); nenhum processo órfão confirmado (`ps`/porta 3014 livre)
- Divergências, falhas e evidências invalidadas: `npm ci` falhou (`EUSAGE`, package-lock.json fora de sincronia com package.json) — usado `npm install --legacy-peer-deps`, mesmo comando de `deploy/docker-compose-tests/*/Dockerfile.test`; `npm install` bloqueou o script de instalação do `sqlite3` por política padrão do npm (`allowScripts`), corrigido e incluído em `package.json` (ver §1 do relatório); a reescrita de `package-lock.json` gerada por esses `npm install` **não foi commitada** (revertida) por ser uma mudança grande de grafo de dependências fora do escopo desta tarefa — registrada apenas como achado no relatório; artefato de teste `deploy/playwright/emailData.json` (mutado pelos próprios testes) também revertido
- Bloqueio e condição de desbloqueio: nenhum bloqueio na execução. Pendências de conteúdo (não bloqueiam IN_REVIEW, mas ficam explícitas): p95/p99 sob carga HTTP não medido (sem ferramenta de load test no repo), RPO/RTO não exercitado, 4 causas-raiz de falha Playwright (`TC_08`, `TC_43` ×3) sem segunda execução de confirmação
- Próximo passo exato e checks para retomada: se a revisão pedir confirmação das falhas Playwright, rodar novamente `deploy/playwright/run.sh` (suíte completa, ~16 min, pois os specs numerados presumem execução em sequência) e comparar; se decidir instrumentar p95/p99, avaliar introduzir uma ferramenta de load test (ex. autocannon) como tarefa própria, não dentro de GO-002
- Recuperação/rollback aplicável: nenhuma mutação fora do ambiente de teste descartável; nada a reverter em caso de falha
- Revisão / decisão de encerramento: pendente de revisão humana e merge — tarefa permanece IN_REVIEW até então, conforme protocolo
